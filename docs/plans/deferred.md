# Deferred 대장

## 대기

- [2026-08-22] **업데이트 다운로드 진행률 표시** — 지금은 `다운로드 중...` 문구뿐이라 몇 MB 중 얼마를 받았는지 알 수 없다. WinHTTP는 `Content-Length`와 읽은 바이트를 이미 쥐고 있어(`app::update::http::read_response`가 그 값을 버린다) 값 자체는 있다. 출처: 2026-08-21-auto-update.
- [2026-08-22] **업데이트 확인 실패를 시작 시에는 알리지 않는다** — 손으로 누른 확인만 결과를 알리고(D7), 앱이 뜰 때 도는 확인이 실패하면 아무것도 보이지 않는다. 켤 때마다 알림이 뜨는 것을 피한 선택이지만, 며칠째 확인이 안 되고 있어도 사용자가 모른다. 써 보고 정한다. 출처: 2026-08-21-auto-update.
- [2026-08-22] **`AGENTS.md`가 SessionStart 주입 상한(16KB)을 넘는다** — 20,601B. 착수 시점에 이미 18,420B로 넘어 있었고 이번 회차가 릴리즈 절차·노트 규약으로 +2,181B를 더했다(초과 자체는 자기 유발이 아니지만 **키운 것은 이번 diff다**). `pjc:record-project-fact`의 「주입 상한 점검·이관」으로 큰 절을 별도 문서로 옮기고 포인터만 남기는 정리가 필요하다. 출처: 2026-08-21-auto-update.
- [2026-08-21] **[SUGGEST] `release_conn`에도 `TransferRunner::forget_connection`을 잇기** — 지금 그 함수는 `ui::app::detach_site`(사이드바 사이트 삭제) 한 곳에서만 불린다. **탭 `✕`로 연결을 접는 길**에서는 취소 뒤 정리를 기다리던 `.part`가 그대로 남는다(그 함수의 doc이 그 상황을 서술한다 — 연결이 닫히면 워커의 완료 통지가 채널째 버려져 `on_done`이 오지 않는다). **착수 전 확인된 것**: ⓐ 부르는 자리는 `release_conn`(`src/ui/app/remote.rs`)의 `manager.close` 직후여야 한다 — 그보다 앞서면 `assigned`가 먼저 비워져 이어지는 `runner.cancel`이 no-op가 된다 ⓑ 이번 회차가 새 경로에서만 부른 이유는 기존 경로(탭 닫기·패널 닫기 2곳)의 회귀 검증이 따로 필요해서다 (출처: 2026-08-21-sidebar-site-delete D8)
- [2026-08-21] **위키 ingest — 사이드바 사이트 삭제 회차** — `20_projects/personal/moa/feat-remote-sites.md:74`의 「숨기기와 삭제는 다르다: 사이드바 우클릭의 `삭제`는 바로가기만 없앤다」가 뒤집혔다. `feat-dock-status.md`·`feat-remote-transfer.md`의 연결별 탭 서술에도 「사이트를 지우면 그 탭이 사라진다」가 더해질 자리가 있다. vault `pending.md`에 `[K-DRIFT]`로 큐잉돼 있어 다음 ingest 세션이 소비한다 (출처: 2026-08-21-sidebar-site-delete F-6.5)
- [2026-08-21] **`eframe` 0.36 상향** — 직접 의존성 중 유일하게 뒤처진 것이며(0.35.0 → 0.36.1) 그 자체의 브레이킹은 작다(제거된 API 중 이 앱이 쓰는 것은 시험 코드의 `RawInput.modifiers` 한 곳, `clip_rect_margin`은 미사용, rustc 1.95로 MSRV 충족). **막는 것은 `egui-phosphor`다** — crates.io 최신판(0.13.0)도 git main도 `egui = "0.35"`에 고정이고 egui 0.36 배포 16일째 대응 커밋이 없다(직전 두 번은 릴리스 다음날 대응했다). **착수 전 확인된 것**: ⓐ 그 크레이트는 매우 얇다 — `res/Phosphor.ttf`(488KB) + 상수 목록 + `add_to_fonts` 10줄이고 egui 의존은 그 함수 하나뿐이다 ⓑ 이 앱이 쓰는 아이콘은 23종(실측) ⓒ 자체 관리로 가면 AGENTS 아이콘 규약·`is_icon_font` 규약 시험·라이선스 자산(Phosphor 글꼴은 이미 번들 항목)이 함께 바뀐다 ⓓ egui 0.36에 「press가 위젯을 떠나면 drag로 본다」는 판정 변경이 있어 이 앱의 드래그 기능에 수동 회귀 검증이 필요하다. **대응판(0.14)이 나오면 둘을 함께 올리는 것이 가장 싸다** (출처: 2026-08-21-license-and-deps D4)
- [2026-08-21] **[SUGGEST] `Cargo.toml`에 `license = "MIT"` 필드 더하기** — `LICENSE` 파일은 뒀지만 매니페스트에는 선언이 없다. crates.io에 발행하지 않는 앱이라 그 필드를 읽는 곳이 지금은 없어 두지 않았다. 도구가 라이선스를 읽는 자리가 생기면 한 줄로 끝난다 (출처: 2026-08-21-license-and-deps F-7 m6)
- [2026-08-21] **[SUGGEST] 시험용 셰이프 텍스트 수집 헬퍼가 여섯 곳에 중복** — `Shape::Vec` 재귀 + `Shape::Text` 수집 패턴이 `ui/about_dialog.rs`(이번 회차가 더한 여섯 번째)·`ui/tabs.rs`·`ui/menu.rs`·`ui/sidebar.rs`·`ui/tree.rs`·`ui/panel/tests.rs`에 각각 손으로 재구현돼 있다. 각 파일이 독립 `#[cfg(test)] mod tests`라 3회 문턱이 파일 간에 그대로 적용되지는 않지만, 시험 전용 공용 모듈(`src/ui/test_support.rs`)로 뽑으면 반복이 준다. **같은 날 대장의 2026-08-20 「`ui_sources` 재귀 헬퍼가 세 곳에 중복」과 해법이 같다**(둘 다 그 모듈 하나면 끝난다) — 함께 다루는 편이 낫다 (출처: 2026-08-21-license-and-deps T4 quality S1, F-7 m2)
- [2026-08-21] **공개 저장소에 남는 개발 메모 정리 여부** — 레포 루트의 `notes.md`·`debug-2026-07-24-dark-ownerdraw.md`와 `notes-archive/`가 커밋돼 있어 GitHub 공개 시 그대로 노출된다. **이번 변경과 인과가 없는 기존 상태**이며 민감 정보가 든 것으로 확인된 바는 없다 — 공개 시점에 남길지 옮길지 사용자 판단이 필요하다 (출처: 2026-08-21-license-and-deps F-7)
- [2026-08-21] **OS(탐색기·바탕화면)에서 끌어온 것을 같은 폴더에 놓았을 때도 취소하기** — 앱 안 드래그는 2026-08-21에 취소로 바뀌었으나(FR-60) OS에서 끌어온 경로는 종전대로 셸이 복사해 `- 복사본`이 생긴다. 사용자가 그 회차에 "이번엔 그대로 둔다"를 골랐다. **착수 전 확인된 것**: ⓐ 두 경로가 갈리는 자리는 `ui::app`이다 — 앱 안 드롭은 `local_copy_target`을 거치지만(`app.rs:2185`) OS 드롭은 `pump_os_drop` → `start_local_copy_paths`로 **그 판정을 통째로 건너뛴다** ⓑ 판정 재료는 이미 있다: 끌어온 경로들의 `parent()`와 대상 폴더를 견주면 되고, 대소문자를 접는 비교기(`ui::list_common::same_dir`)도 그 자리에 있다(비공개라 공개 범위를 넓혀야 한다) ⓒ **PRD FR-61 ⓐ 문면 개정이 함께 필요하다** — 지금 FR-60이 「OS에서 끌어온 것은 이 판정 대상이 아니다」로 명시하고 있어 그 구를 함께 고쳐야 한다 (출처: 2026-08-21-drag-cancel-preview-dock)
- [2026-08-21] **[SUGGEST] 32bpp 비트맵 읽기 절차가 세 곳에 중복** — `fs::thumbnail::bitmap_to_rgba`·`ui::icon_tex::color_bitmap_to_image`·`fs::drag_image::read_bgra` 셋이 `GetObjectW` → 헤더 세우기 → `CreateCompatibleDC` → `GetDIBits` → `DeleteDC`를 같은 순서로 되풀이한다(공통화 문턱 3회에 닿았다). **착수 전 확인된 것**: ⓐ 세 곳의 *읽기* 구간은 절차가 같고 갈리는 것은 그 뒤다 — 앞의 둘은 RGBA `Vec`을 돌려주고 `drag_image`는 BGRA를 그대로 DIB 섹션에 쓴다 ⓑ 그래서 합치려면 **반환 형태를 함께 손봐야 한다** ⓒ 공통 위치는 `fs` 쪽이어야 한다(`ui::icon_tex`가 `fs`를 알지 그 반대가 아니다) ⓓ 프리멀티플라이 되돌리기 규칙(알파 전부 0이면 불투명으로 채운다)도 세 곳이 같다 (출처: 2026-08-21-drag-preview-image T2 quality S1)
- [2026-08-20] **원격 항목을 탐색기로 끌어내기** — FR-61 내보내기는 로컬 항목만 대상이다(원격은 끌기 시작 시점에 로컬에 파일이 없다). **착수 전 확인된 것**: ⓐ **PRD 개정이 먼저다** — FR-61 본문이 「**로컬 항목만** 내보내며 원격 항목은 대상이 아니다」로 못박고 있다 ⓑ 지금은 셸이 만들어 주는 `IDataObject`를 쓰지만(`fs::drag_source`의 `SHCreateShellItemArrayFromIDLists` → `BindToHandler`) 지연 렌더링은 **`IDataObject`를 직접 구현**해야 한다(`windows` crate에 `IDataObject_Impl` 트레이트 실재 — `Win32/System/Com/mod.rs:4746`) ⓒ 형식 상수 둘 다 실재한다 — `CFSTR_FILEDESCRIPTORW`·`CFSTR_FILECONTENTS`(`Win32/UI/Shell/mod.rs:6447`·`:6444`) ⓓ **가장 큰 설계 쟁점**: 받는 쪽이 `IStream`을 당기는 순간 서버에서 내려받아야 해 전송 큐와 얽힌다 — 그 자리를 어떻게 이을지가 이 작업의 본체다 (출처: 2026-08-20-drag-copy-and-transfer-relist)
- ~~[2026-08-20] **드래그 중 미리보기 그림** — 끌고 있는 항목의 반투명 썸네일. **착수 전 확인된 것**: ⓐ `IDragSourceHelper`가 `windows` crate에 실재한다(`Win32/UI/Shell/mod.rs:19025`, `IDragSourceHelper2`도 있다) ⓑ `DoDragDrop` **앞에서** 그 헬퍼를 초기화하고 데이터 객체에 붙인다 ⓒ **지금 코드가 이미 전제를 갖췄다** — `fs::drag_source`의 `GiveFeedback`이 `DRAGDROP_S_USEDEFAULTCURSORS`를 돌려주는데, 헬퍼의 이미지가 보이려면 바로 그 값이어야 한다 ⓓ 다만 지금은 셸이 만든 `IDataObject`를 쓰므로 헬퍼가 요구하는 `SetDragImage` 호출을 그 객체에 걸 수 있는지 확인이 필요하다 (출처: 2026-08-20-drag-copy-and-transfer-relist)~~ → **2026-08-21 해소**: `fs::drag_image`가 첫 항목의 그림을 만들고 `fs::drag_source`가 `IDragSourceHelper`에 얹는다. ⓓ의 걱정(셸이 만든 `IDataObject`가 헬퍼의 사설 형식 `SetData`를 받는가)은 **2026-08-21 수동 검증에서 닫혔다** — 그림과 드롭 설명 문구(`DSH_ALLOWDROPDESCRIPTIONTEXT`)가 실제로 뜨고, 20회 반복에도 GDI 개체가 늘지 않아 「얹은 비트맵은 지우지 않는다」는 판단도 함께 확인됐다
- ~~[2026-08-20] **[SUGGEST] `views → panels` 이중 순회가 세 곳에 반복** — `src/ui/app/remote.rs`의 `list_moved_panels`·`request_remote_list`·`pump_relist`. 공통화 문턱 3회에 닿았고 조건만 받는 순회 헬퍼로 묶을 수 있다 (출처: 2026-08-20-drag-copy-and-transfer-relist T2 quality S1)~~ → **2026-08-20 해소**: 조건을 클로저로 받는 `ExplorerApp::relist_panels` 하나로 모았다(세 호출부의 다른 점은 조건뿐이라 호출부에 그대로 남는다). 옮김 판정이 깃발을 거두는 조작이라 조건은 `&mut PanelState`를 받는다
- [2026-08-20] **`ExplorerApp`의 채널·상태 필드가 또 늘었다** — 2026-08-20 회차가 `relist`·`copy_tx`/`copy_rx`·`os_drop_tx`/`os_drop_rx` 다섯을 더했다. 같은 날 등록된 `[SUGGEST] 충돌 상태 6개 필드를 ConflictState로 묶기`와 **대상이 겹치지는 않지만 같은 범주가 커졌으므로 한 회차에 함께 다루는 편이 낫다**(따로 하면 같은 파일을 두 번 흔든다). **착수 전 확인된 것**: 캡슐화 선례는 `panel/workers.rs`의 `DirLoad`이고, 묶을 후보는 ⓐ 충돌 확인 6개(`conflict_*`·`pending_conflicts`·`next_conflict`) ⓑ 드래그·복사 채널 3쌍(`copy_*`·`os_drop_*`·`expand_*`) 두 덩어리다. **구조 변경이라 승인 대상**이며 호출부가 여러 곳 바뀐다 (출처: 2026-08-20-drag-copy-and-transfer-relist)
- ~~[2026-08-20] **기존 소스 세 곳에 BEL(0x07) 제어 문자** — `src/app/settings.rs`·`src/remote/queue.rs`·`src/ui/tabs.rs`(및 `src/ui/queue_panel.rs`)의 시험 픽스처 경로에서 백슬래시+a가 실제 BEL 바이트로 들어가 있다. **master에도 있어 이번 회차와 인과가 없다**(같은 회차가 만든 같은 오류는 `app/remote.rs`에서 고쳤다). 시험 결과에는 영향이 없으나 소스에 제어 문자가 남는 것은 정리 대상이다 (출처: 2026-08-20-drag-copy-and-transfer-relist Phase F)~~ → **2026-08-20 해소**: 실측은 다섯 파일 일곱 곳이었고(BEL 넷·BS 둘·FF 하나 — ``·``·``가 이스케이프되어 박힌 것) 전부 의도한 경로·문구로 되돌렸다. `src/ui/address_bar.rs`의 주석 한 곳은 시험 픽스처가 아니라 설명문이었다
- [2026-08-20] `ui/app.rs`의 시험(현 18건)을 `app/tests.rs`로 뺄지 — `panel/tests.rs` 선례가 있다. 이번 회차는 **옮긴 자유 함수의 시험만** 따라 보냈고(충돌 7건 → `app/transfer_conflict.rs`, 원격 6건 → `app/remote.rs`) 나머지는 `app.rs`에 남겼다 (출처: 2026-08-20-app-rs-split D7, F-7 M1이 대장 누락을 잡음)
- [2026-08-20] **재현하지 못한 `cargo test` 간헐 실패 1건** — 2026-08-20-site-export-import의 Phase F-2 첫 실행에서 `899 passed; 1 failed`가 났는데 **실패한 시험 이름을 잡지 못했고**(출력에서 놓쳤다) 이후 lib 6회 + 전체 8회, 합 14회 연속 통과로 재현되지 않았다. 같은 날 해소한 `toast.rs` 잠금 누락과는 다른 건이다(그것은 그 실행 이전에 이미 고쳐져 있었다). 2026-08-15 항목(「CPU가 붐빌 때 lib 시험 1건이 간헐 실패」)과 같은 것일 수 있으나 단정할 근거가 없다. **다음에 실패를 만나면 이름부터 확보할 것** — `cargo test 2>&1 | tee`로 로그를 남기고 돌린다
- [2026-08-20] **[SUGGEST] `ui_sources` 재귀 헬퍼가 세 곳에 중복**(`theme.rs`·`dialog.rs`·`widgets.rs`, 바이트 단위로 동일) — 공통화 문턱 3회를 정확히 채웠다. plan이 든 보류 사유(「`#[cfg(test)]` 가시성 배선」)를 **quality 리뷰가 「과장됐다」고 지적했다** — `#[cfg(test)] pub(crate) fn`을 공통 모듈에 두는 것은 표준 패턴이다. 다만 이 레포에 그런 test-support 모듈 관례가 없어 신규 모듈을 여는 비용은 실재한다. 제안 형태: `src/ui/mod.rs`에 `#[cfg(test)] pub(crate) mod test_support;`. **네 번째 시험이 같은 헬퍼를 쓰게 되면 그때는 공용화가 명백하다** (출처: 2026-08-20-app-rs-split T3 quality m1)
- [2026-08-20] **[SUGGEST] `app/remote.rs`(1038줄)를 손자 모듈로 더 쪼개기** — 세 관심사가 한 `impl` 블록에 있다(①연결 수명주기 ②원격 메뉴 대화 ③이벤트 폴링·트리 조회). 네 질문의 ①③이 「예」에 가깝다. **`pub(super)`는 `ui::app`의 후손 전체에 보이므로 손자 모듈(`remote::menu`·`remote::poll` 등)로 쪼개도 가시성 근거가 유지된다.** 이번엔 순수 이동 범위를 지켜 하지 않았다 (출처: 2026-08-20-app-rs-split T2 quality m1)
- [2026-08-20] **[SUGGEST] 충돌 상태 6개 필드를 `ConflictState`로 묶기** — `pending_conflicts`·`conflict_tx/rx`·`conflict_lists`·`conflict_queue`·`conflict_dialog`·`next_conflict`를 모으면 `panel/workers.rs`의 `DirLoad`와 같은 캡슐화가 되고 자식 모듈에 노출되는 부모 필드가 6 → 1로 준다. 상태 필드 이동은 구조 변경이라 「순수 이동」 회차에서는 하지 않았다 (출처: 2026-08-20-app-rs-split T1 quality S1)
- [2026-08-20] `ExplorerApp::connect_site`를 `pub(super)`로 좁힐지 — 호출부가 `app/remote.rs` 안 3곳뿐이라 좁힐 수 있고 그러면 이동 항목 36개가 모두 `pub(super)`로 일관된다. **이번엔 좁히지 않았다** — plan 4-B·D5가 「좁히는 것도 계약 변경이라 이동 범위 밖」으로 정했고, 구현 중 실수로 좁혔던 것을 spec 리뷰가 BLOCKER로 잡아 되돌렸다. 다음에 다룰 때는 그 계약 변경을 명시적으로 승인받아야 한다 (출처: 2026-08-20-app-rs-split T2 quality m2)
- [2026-08-20] 트리 메뉴·원격 메뉴의 화면 밖 보정 크기를 **실측으로 바꾸기** — 종전 항목이 「원격 메뉴처럼 실측 상수로」라고 적었으나 조사 결과 **원격 쪽도 어림값**이라(`remote_menu.rs`의 `FRAME_PAD = 8.0`, 주석에 "어림한 값") 그 방향은 성립하지 않는다. 둘 다 실측으로 가려면 프레임 테두리·여백을 egui 스타일에서 읽어야 한다 (출처: 2026-08-19-small-refactors, 종전 2026-08-17 항목을 사실에 맞게 다시 세움)
- [2026-08-20] `theme::OK_BAR`와 `theme::OK_BORDER`가 **값이 같다**(`0x2F6B4F`) — 역할이 달라(막대 채움 vs 상태 배지 테두리) 이번엔 합치지 않았다. 팔레트를 손볼 때 다시 볼 자리다 (출처: 2026-08-19-small-refactors D2)
- ~~[2026-08-19] 정보 팝업에 저작권 줄·홈페이지 링크 더하기 — 이번엔 사용자가 아이콘 + 이름·버전만인 최소 구성을 골랐다. 줄만 더하면 되는 자리다 (출처: 2026-08-19-about-dialog)~~ → **2026-08-21 해소**: 저작권·라이선스·저장소 세 줄을 더했다(FR-58 개정). 저장소 줄은 `ui.hyperlink_to`로 눌러 열 수 있고, 세 줄 다 `i18n` 카탈로그를 거친다(두 언어에서 값은 같다). 이 프로젝트를 MIT로 공개하면서 함께 처리했다
- [2026-08-19] 앱 이름 `"MOA"` 상수 통합 — **대상이 둘로 갈렸다(2026-08-19 정정)**: `tray`(툴팁)·`main`(창 제목)은 **화면에 보이는 이름이라 i18n 카탈로그(`i18n::app_name`)로 옮겨 해소**됐다(한국어 `모아` / 영어 `MOA`). 남은 것은 **데이터가 걸린 셋** — `autostart`(레지스트리 값 이름)·`settings`·`remote::hostkey`(폴더 이름)이며, 이들은 언어를 따라가면 안 되고 바꾸면 자동 실행 등록·기존 설정·호스트 키를 잃는다. 통합한다면 그 셋만 한 상수로 모으는 것이다 (출처: 2026-08-19-about-dialog D8, 2026-08-19-app-name-icon-menus T1이 앞의 둘을 해소)
- [2026-08-19] 라이선스 대화의 목록 검색·필터 — 구성 요소가 158개라 이름으로 좁히면 편하다. 목록이 정렬돼 있고 스크롤로 닿아 없이도 성립한다 (출처: 2026-08-18-open-source-licenses)
- [2026-08-19] 라이선스 종류별 묶어 보기(같은 전문을 쓰는 크레이트를 한 항목으로) — 자산이 이미 전문을 중복 제거해 담으므로 화면만 바꾸면 되는 확장 지점이다 (출처: 2026-08-18-open-source-licenses)
- [2026-08-17] **아이콘 재시도가 성공으로 회복되는 경로에 시험이 없다** — `IconTextures::convert_into`의 성공 분기(`retries.remove` + `created_this_frame` 증가)가 **재시도 맥락에서는 한 번도 실행되지 않는다**. 신설 시험 5건이 모두 음수 인덱스(결정론적 실패)를 쓰기 때문이며, **주입 지점 없이는 일시적 실패를 만들 수 없다**(`icon_to_image`가 자유 함수라 갈아 끼울 수 없다). "다시 시도된다"까지는 시험이 지키지만 "복원된다"는 미검증이다 — 필요해지면 변환을 시험에서 대체 가능하게 만드는 별도 회차 (출처: 2026-08-17-icon-texture-lock F-7 m2)
- [2026-07-23] 트리→목록 양방향 동기화 (출처: 2026-07-23-file-explorer-part2 D14, egui 이식 후에도 단방향 유지)
- [2026-07-28] 구 Win32 UI 코드 제거 — 전체 삭제 7개(app/{window,sidebar,menu,layout_host}.rs·panel/{panel,folder_tree,address_bar}.rs)와 부분 삭제 3개(panel/{file_list,tabs}.rs의 Win32 래퍼, app/theme.rs의 COLORREF 상수). enable_dark_mode는 존치. ~~사용자가 화면 확인 후 판단하기로 보류~~ → **2026-08-20 그 전제가 풀렸다**: 사용자가 화면 확인 18항목을 마쳤고 전부 정상이었다. **이제 착수 판단만 남았다** (출처: 2026-07-26-egui-migration-part2 T7)
- [2026-07-28] 한글 폰트 서브셋 — egui 메모리의 큰 몫(약 27MB 추정). NFR-2에 여유가 있어 지금은 불필요하나 더 줄여야 하면 첫 후보 (출처: 2026-07-26-egui-migration-part2)
- [2026-07-28] `app/`·`panel/`에 남는 순수 로직의 모듈 재배치(예: `core/`) — Win32 코드 제거 후에 검토 (출처: 2026-07-26-egui-migration-part2)
- [2026-07-28] 사이드바 마지막 항목 뒤 여백 4px — `show_items`가 항목마다 간격을 붙여 마지막 카드 뒤에도 남는다. 화면상 문제는 없음 (출처: 2026-07-26-egui-migration-part2 T2 quality 리뷰 m2)
- [2026-07-28] `debug-2026-07-24-dark-ownerdraw.md` 루트 위치 정리 — `docs/`로 옮길지 결정 (출처: 2026-07-26-egui-migration-part1)
- [2026-07-28] master 미병합 커밋 다수 — 다크 테마·사이드바·PoC·egui 이식이 모두 `task/*` 브랜치에 쌓여 있다. 병합 전략을 사용자와 결정해야 한다 (출처: 2026-07-26-egui-migration-part2)
- [2026-07-28] 워크스페이스 Delete 키 배정 — 사이드바가 키를 전역으로 보는 현재 구조에서는 파일 목록에서 누른 Delete까지 워크스페이스를 지운다. 키 미배정 + 메뉴 표기 제거로 처리했고, 카드에 포커스를 주는(`has_focus`) 방식으로 전환할 때 되살린다. F2도 같은 성질 (출처: 2026-07-26-egui-migration-part2 F-7 M2)
- [2026-07-28] 워크스페이스 키보드 위/아래 전환 — 현행 Win32 판에 있던 것이 이식에서 빠짐. PRD 요구는 아님 (출처: 2026-07-26-egui-migration-part2 F-7)
- [2026-07-29] 커스텀 타이틀바의 창 그림자·둥근 모서리 — winit의 무장식 그림자 확장을 eframe이 노출하지 않아 이번엔 포기. `DwmSetWindowAttribute`의 코너 설정으로 모서리만 되살릴 여지는 있다 (출처: 2026-07-29-custom-titlebar D10)
- [2026-07-29] 탭 폭 고정(Windows 11은 탭마다 같은 폭) — 현재는 제목 길이에 맞춘다. 좁은 분할 패널에서 고정폭이 나은지 화면을 보고 판단 (출처: 2026-07-29-tab-strip-and-pane-borders)
- [2026-07-28] T6 측정 재현 절차 — 워크스페이스 5개 메모리는 임시 측정 빌드로 얻었고 스크립트를 레포에 남기지 않아 재현에 같은 패치가 필요하다 (출처: 2026-07-26-egui-migration-part2 F-7 m5)

- [2026-07-29] 자세히 보기의 고정 헤더 — 가로 스크롤을 위해 헤더를 본문과 같은 `ScrollArea`에 넣어, 세로 스크롤 시 머리글이 함께 올라간다. 세로만 고정하려면 두 영역의 오프셋을 수동 동기화해야 한다 (출처: 2026-07-29-view-modes-and-panel-menu T2)
- [2026-07-29] 보기 모드별 마지막 정렬 기준 기억 — 지금은 모드를 바꿔도 정렬이 유지된다 (출처: 같은 plan)
- [2026-07-29] 자세히 보기 열 추가·제거·순서 변경 — 이번에 열 메타데이터 구조가 생겨 확장 지점이 열렸다 (출처: 같은 plan)
- [2026-07-29] `list_grid::show`의 `visible` 부수 출력 — 즉시 모드에서 렌더 도중 `&mut PanelState`를 잡을 수 없어 택한 절충. 렌더와 수집을 나누는 구조가 있으면 검토 (출처: 같은 plan T14 quality m1)
- [2026-07-29] `PanelState::from_tabs` 인자 증가(4개) — 호출부가 이미 `PanelTabs`를 갖고 있어 그것을 넘기면 필드가 늘어도 시그니처가 안 바뀐다 (출처: 같은 plan T12 quality S1)
- [2026-07-30] ~~NFR-1 위반~~ **→ 해소: 콜드 스타트 0.4~0.6초로 기준(1초) 충족.** 한때 1.2~10.3초로 측정돼 "위반"으로 보고했으나, `log` 로거로 eframe 내부를 들여다보는 단계에서 **같은 조건이 0.3~0.7초로 재측정**됐고 이후 어떤 방법으로도 느린 값이 재현되지 않았다(순정 릴리즈·빌드 직후 즉시·exe 새 복사본·간격 3/6/20/60초 — 24회 이상 전부 0.3~0.7초).
  - **느린 수치의 정체는 특정하지 못했다.** 창 옵션(4조합)·실행 간격(3~60초)·끊긴 네트워크 드라이브·Defender 첫 스캔(복사본 첫 실행 663ms vs 이후 356ms — 영향 미미)·빌드 직후 상태를 모두 측정으로 배제했다. 그 시점의 일시적 시스템 부하로 보이며, **재발하면 아래 구간 내역과 대조해 다시 좁힌다.**
  - **초기화 구간 내역** (`log` 로거로 실측 — 여유를 아는 기준선):
    | 구간 | 소요 |
    |---|---|
    | `main`~`run_native` 호출 (COM·세션·다크 정책) | 4~14ms |
    | winit 이벤트 루프 진입 | 5~16ms |
    | **glutin Display 생성 (WGL 픽셀 포맷 열거)** | **260~420ms — 전체의 대부분** |
    | GL 컨텍스트·surface·egui_glow painter | 약 50ms |
    | `install_fonts`(맑은 고딕 12.84MB)·`IconCache` | 약 36ms |
  - 더 줄여야 할 일이 생기면 손댈 곳은 **glutin Display 생성**뿐이고, 그것은 Intel Arc 드라이버의 WGL 초기화라 앱 코드로는 건드릴 수 없다(백엔드 교체는 NFR-2와 충돌 — wgpu는 PoC 실측 359MB vs glow 131MB).
  - 계측 패치는 커밋하지 않았다 — 재현에는 `log = "0.4"` 임시 의존성 + `log::Log` 구현체를 `main.rs`에 두고 `FE_PROBE_FILE`로 출력 경로를 받는 방식이 필요하다(eframe/winit/glutin이 내는 DEBUG/TRACE가 그대로 찍힌다). (출처: 2026-07-29-view-modes-and-panel-menu F-8 남은 이슈 조사)
- [2026-07-30] NFR-3의 프레임 시간 미측정 — 10만 파일 폴더에서 "응답 유지·모드 전환 2.5~2.8초"까지는 외부 스크립트로 확인했으나, 프레임당 소요 시간은 앱 내부 계측(`ctx.input(|i| i.stable_dt)`) 없이는 잴 수 없다. 상시 표시할 성질이 아니므로 디버그 오버레이나 임시 계측으로 다룰지 결정 필요 (출처: 같은 조사)
- [2026-08-05] **원격 파일명 보내는 쪽의 문자셋** — 받는 쪽(LIST 원문 디코딩)은 해결됐다. 보내는 쪽은 `suppaftp`가 명령 인자를 `String`으로만 받아 CP949 바이트를 실을 수 없다(Rust `String`은 UTF-8 불변식). `OPTS UTF8 ON`을 받아들이는 서버는 문제없고, 그렇지 않은 서버에서 **한글 폴더로 들어가기·이름 바꾸기**가 막힌다. 라이브러리 포크 또는 제어 소켓 직접 쓰기가 필요 (출처: 2026-08-04-ftp-integration T34)
- [2026-08-05] SFTP 목록의 비UTF-8 파일명 — `ssh2`가 Windows에서 경로를 UTF-8로 단정해 패닉한다. `guard_path_panic`이 오류로 바꿔 연결은 지키지만 이름을 읽어 보이지는 못한다 (위 문자셋 항목과 같은 뿌리, 출처: 2026-08-04-ftp-integration T3)
- [2026-08-05] `· TLS` 표기가 협상 결과가 아니라 설정 의도다 — 서버가 TLS를 거부해 평문으로 가도 상태 줄은 `· TLS`라고 적는다. 세션이 협상 결과를 올리도록 넓히는 별도 task 필요 (출처: 2026-08-04-ftp-integration T21)
- [2026-08-05] 원격 목록의 변경 감시(로컬 FR-10의 원격 판) — FTP·SFTP에 표준 통지가 없어 폴링뿐이고 서버 부하가 커 수동 새로 고침만 뒀다 (출처: 2026-08-04-ftp-integration 범위 결정)
- [2026-08-05] 사이트 관리자 `전송 설정`·`문자셋` 탭 재디자인 반영 · 단축키 `Ctrl+S` — 디자인 README가 재디자인을 예고했고 단축키는 PRD·Acceptance에 없어 넣지 않았다 (출처: 2026-08-04-ftp-integration T15·T16)
- [2026-08-05] `ui/app.rs`(**2356줄 — 2026-08-20 실측**, 회차 전 3802줄)의 **남은 책임 분리** — 판정 근거는 2026-08-20에 「1500줄 검토선」에서 네 질문으로 갈아 끼웠다(그 임계는 pjc 규약이 폐기했고 AGENTS.md도 같은 날 바꿨다 — **줄 수는 분리 근거가 아니다**). **원격 배선과 전송 충돌 확인 두 책임은 2026-08-20에 자식 모듈로 나갔다**(`app/remote.rs` 1038줄 · `app/transfer_conflict.rs` 487줄, plan `2026-08-20-app-rs-split`). 남은 책임은 **세션·워크스페이스 211줄 · 그 밖 화면 206줄 · 도크·큐·로그 184줄 · 창·트레이 173줄 · 명령 배선 112줄 · 전송 대상 판정 21줄**(각 실측)이며 ①이 여전히 걸린다 — 다만 덩어리가 작아져 다음 분리의 이득이 이번보다 작다. **`ui/panel.rs`(1645줄)·`ui/site_manager.rs`(1869줄)는 이 항목 밖이다** — 줄 수만으로 묶여 있었을 뿐 네 질문을 적용한 적이 없고, 유지 판정이 나올 수도 있다 (출처: 2026-08-04-ftp-integration quality 리뷰, 2026-08-20 근거 교체·진행 반영)
- [2026-08-05] 원격 조회 요청에 **패널 식별자** — 같은 연결을 나눠 쓰는 두 패널이 같은 세대로 같은 경로를 향해 동시에 대기하면 한쪽의 실패가 다른 쪽도 되돌린다(되돌린 뒤 경로·목록은 서로 맞아 파괴적 위험은 없다). 아래 출처 태그 항목과 한 뿌리 (출처: 2026-08-04-ftp-integration F-7 3라운드 m2)
- [2026-08-05] 원격 조회 요청에 출처 태그 — 트리 조회 세대를 `1<<40`부터 세어 목록 조회와 번호 공간을 나눈다. 요청 종류가 셋이 되면 `enum ListSource`로 바꾼다 (출처: 2026-08-04-ftp-integration T24 quality m2)
- [2026-08-05] 패널 목록 조회 실패를 화면에 잇기 — T24가 넣은 `ConnEvent::ListFailed`를 지금은 트리만 소비한다 (출처: 2026-08-04-ftp-integration T24 quality S1)
- [2026-08-05] 디자인 HTML의 샘플 호스트가 실제 IP 형태 — 사용자 제공 기준 문서라 손대지 않았다. **파일을 지금 고쳐도 값은 커밋 `b5bedf3`에 남는다** — 이력에서 떼려면 master로 **squash merge**하거나 history rewrite(별도 승인 대상)가 필요하다. push 승인 요청 때 이 선택지를 함께 제시한다 (출처: 2026-08-04-ftp-integration T0 관측, F-7 리뷰 M4)
- [2026-08-14] 유휴 메모리가 2026-08-05 대비 45MB 증가(173.8MB — NFR-2 150MB 초과) — 같은 조건에서 이 기능이 없는 `6fe19e0`도 172.9MB라 이번 작업과 무관함은 밝혔으나 무엇이 늘렸는지는 미상. 그래픽 드라이버·OS 상태를 통제한 조건에서 재측정 필요 (출처: 2026-08-13-app-settings-part1 T13)
- [2026-08-14] 숨김 항목 토글이 원격 패널에서도 재조회를 부른다 — 로컬은 감시 갱신 때마다 어차피 다시 읽지만 원격은 네트워크 왕복이다. 원격만 응답을 캐시해 재필터하는 길이 있다 (출처: 2026-08-13-app-settings-part1 T12 quality S1, 2026-08-18 재확인: 유지 — 시스템 파일 토글이 갈라지며 헛도는 경우가 하나 늘었다)
- [2026-08-14] **오류 사유가 언어 전환을 따라가지 않는다** — 실패한 순간의 언어로 굳어 화면에 남고(전송 큐의 것은 세션에 저장돼 재시작 뒤에도), 연결 실패 화면에서는 한 줄에 두 언어가 섞인다. 고치려면 오류를 문자열이 아니라 구조로 들어야 하는데 10개 파일 + 세션 저장 형식에 걸쳐 **이번엔 PRD FR-53에 한계를 적는 쪽을 골랐다**(2026-08-14 사용자 결정). 되돌린다면 `ConnPhase::Failed`·`TabPhase::Error`·`TransferState::Error`가 `RemoteError`를 들게 하는 것부터다 (출처: 2026-08-13-app-settings-part2 Phase F)
- [2026-08-14] 화면 문구 검사는 **리터럴만 본다** — 문구를 변수에 담아 옮기거나 바깥에서 받아 그리면 걸리지 않는다. 그런 우회를 막는 장치는 없고 리뷰가 본다 (출처: 2026-08-13-app-settings-part2 T8)
- [2026-08-15] PRD FR-32·README의 "폴더 아이콘" 서술에 FR-54 포인터 한 절 — FR-38·FR-39에는 붙였으므로 관례상 여기도 붙이는 편이 낫다. **PRD 문면 변경이라 사용자 승인 필요** (출처: 2026-08-15-transfer-target-tabs 완료 검증 m2)
- [2026-08-15] `conflict_names`가 항목 **종류를 보지 않고 이름만** 대조 — FR-55 문면은 종류 일치를 함의한다. 한 폴더에 같은 이름의 파일과 폴더가 함께 있을 수 없어 동작은 같지만 표현이 다르다 (출처: 2026-08-15-transfer-target-tabs m3, 결함으로 보지 않음)
- [2026-08-15] CPU가 붐빌 때 lib 시험 1건이 간헐 실패 — 시간 마감(2초)에 기대는 시험은 `remote/manager.rs:248·306`·`remote/transfer.rs:454`·`fs/thumbnail.rs:575`뿐이고 셋 다 그 회차가 건드리지 않은 파일이다. 재현되면 마감을 늘리거나 시계를 주입식으로 (출처: 2026-08-15-transfer-target-tabs Phase F)
- [2026-08-15] 충돌 확인의 **재귀 검사** — 고른 최상위 항목 이름만 본다. 폴더를 덮어쓰기로 고르면 그 안의 파일은 개별 확인 없이 덮어쓴다 (출처: 2026-08-15-transfer-target-tabs D4)
- [2026-08-15] 올리기 충돌 확인의 원격 목록 신선도 — 조회와 전송 사이에 서버가 바뀌면 알 수 없다. FTP/SFTP에 변경 통지가 없다는 기존 한계와 한 뿌리 (출처: 2026-08-15-transfer-target-tabs)
- [2026-08-15] 팝업 본문 폭이 대화마다 제각각이다(360·420·460·480·1080) — 이번엔 폭을 건드리지 않기로 해 그대로 뒀다. 소형 400 / 중형 480처럼 규격화할 여지 (출처: 2026-08-15-dialog-style Deferred)
- [2026-08-17] 즐겨찾기가 가리키는 폴더가 사라졌을 때의 표시(흐린 글씨·자동 정리) — 지금은 눌렀을 때 기존 실패 안내로만 알린다. 시작할 때 전건을 확인하면 네트워크 드라이브에서 창이 늦게 뜬다 (출처: 2026-08-16-tree-favorites)
- [2026-08-17] 즐겨찾기 순서 바꾸기(드래그 정렬) — 항목이 많아지면 필요해진다 (출처: 2026-08-16-tree-favorites)
- [2026-08-17] `remote/connection.rs:1033`도 시간 마감(2초)에 기대는 시험이다 — 2026-08-15 항목의 목록(`remote/manager.rs:248·306`·`remote/transfer.rs:454`·`fs/thumbnail.rs:575`)에 이 자리를 더한다. CPU가 붐빌 때 간헐 실패하며 단독 실행은 통과한다 (출처: 2026-08-16-tree-favorites 관측)
- [2026-08-17] 원격 트리가 "모든 줄에 같은 폴더 아이콘"이라는 것을 관측하는 시험이 없다 — `draw_remote`가 `TreeOutcome`만 돌려주고 셰이프를 보지 않아, 원격에 경로별 조회를 끼워도 아무 시험이 붉어지지 않는다 (출처: 2026-08-17-tree-icons F-7 m2)
- [2026-08-17] `IconCache::shell_display_name`이 실패(빈 이름·조회 실패)를 캐시하지 않아 그 경로는 호출마다 셸을 다시 문다 — 지금 호출부는 시작 시 1회씩이라 실피해가 없다 (출처: 2026-08-17-tree-icons F-7 m3)
- [2026-08-17] PRD FR-53("고르면 모든 화면 문구가 바뀐다")의 예외에 **셸에서 온 문자열**이 없다 — 목록 `종류`(`icons::type_name`)와 이번에 생긴 드라이브 표시 이름·기본 즐겨찾기 이름은 앱 언어가 아니라 윈도우 표시 언어를 따른다. 한 회차에서 문면을 함께 정리하는 편이 낫다 (출처: 2026-08-17-tree-icons F-7 m5)

- [2026-08-18] **egui의 끌기 판정을 시험에서 재현하는 방법** — `RawInput`에 `PointerMoved`+`PointerButton{pressed}`를 넣고 시간·프레임을 진행시켜도 `Response::dragged()`가 서지 않았다(두 차례 실측). 그래서 열 폭 드래그의 **화면 쪽**(가이드 선이 서는가)은 큐 표·파일 목록 양쪽 다 수동 검증에 기댄다. 방법을 찾으면 `queue_panel`·`list_details` 두 곳에 함께 넣는다 — 폭 계산 규칙 자체는 `apply_drag` 단위 시험이, 상시 구분선은 셰이프 시험이 이미 고정한다 (출처: 2026-08-18-queue-menus-and-size-display T5)
- [2026-08-18] 큐 표 열의 **표시·숨김·순서 바꾸기** — 이번에 열 폭 상태(`QueueColumns`)가 생겨 확장 지점이 열렸다. 파일 목록의 같은 항목(2026-07-29 등재)과 한 뿌리 (출처: 2026-08-18-queue-menus-and-size-display)
- [2026-08-18] **상단 도크 탭 건수는 연결별 탭(site 축)을 보지 않는다** — `LG` 탭을 고른 채 `실패 (3)`인데 목록이 빌 수 있어, 사용자가 신고한 "목록이 없는데 개수가 표시됨"과 같은 형태가 site 축에 남는다(`dock.rs`의 `count(All/Done/Error)`는 `state.site`를 반영하지 않는다). **2026-08-18 회차에서는 사용자가 대상을 아래 연결별 탭 줄로 좁혀 범위 밖이었다** — 고치려면 `DockView`에 고른 사이트를 넘겨 `visible_items`와 같은 축으로 세면 된다 (출처: 2026-08-18-queue-menus-and-size-display F-7 m4)
- [2026-08-17] 끊긴 네트워크 드라이브의 **주기적 재확인** — 지금은 시작 시 1회 + 그 드라이브를 열어 볼 때만 갱신한다(2026-08-17 사용자 결정). 연결이 되살아나도 한 번 열어 보기 전에는 X 배지가 남는다. 주기 확인을 넣으면 끊긴 드라이브당 0.1초 안팎의 워커 작업이 계속 돈다 (출처: 2026-08-17-network-drive-status)
- [2026-08-17] 트리 **하위 폴더**(드라이브 아래 줄) 아이콘 조회가 여전히 UI 스레드에서 돈다 — 이번 회차가 워커로 옮긴 것은 드라이브 줄뿐이다(`show_node`의 `drive: Some(_)` 갈래). 하위는 그 드라이브를 이미 읽은 뒤라 느릴 이유가 적어 미뤘다. 즐겨찾기 줄도 같은 `icon_for`를 쓴다 (출처: 2026-08-17-network-drive-status T4)

- [2026-08-18] 라이선스 목록의 **검색·필터** — 158개라 이름으로 좁히면 편하다. 이번 요구는 「고지를 볼 수 있게」였고 정렬된 목록을 스크롤로 닿을 수 있어 없이도 성립한다 (출처: 2026-08-18-open-source-licenses)
- [2026-08-18] 라이선스 **종류별 묶어 보기**(같은 전문을 쓰는 크레이트를 한 항목으로) — 자산이 이미 전문을 중복 제거해 담으므로 화면만 바꾸면 되는 확장 지점이다 (출처: 같은 plan)
- [2026-08-18] 라이선스 전문 라벨이 **파일 이름에 기대는 구간이 남아 있다** — `GPLv2`·`LIBM-MIT`·`UNICODE`·`BSD`는 `normalize_label`의 매핑에 없어 그대로 화면에 뜬다. 표기가 갈리는 것(`APACHE`·`mit` 등)만 맞췄고 나머지는 파일 이름이 더 정확하다고 보았다. 라이선스 텍스트를 실제로 판별하는 길이 생기면 다시 볼 자리. **폴백이 라벨을 넓히는 구간도 있다** — `LICENSE`·`COPYING`처럼 종류를 담지 않은 이름이면 그 크레이트의 **선언 전체**를 붙이므로, `miniz_oxide`의 MIT 본문에 `MIT OR Zlib OR Apache-2.0`이, `moxcms`·`pxfm`의 BSD 본문에 `BSD-3-Clause OR Apache-2.0`이 붙는다(틀린 라이선스를 붙이는 것은 아니고 표기가 넓어진다) (출처: 같은 plan T6 · F-7 1회차 m1 · 2회차 m1)

## 종결

- [2026-08-21 → 2026-08-22] 릴리즈·태그 규약 문서화 → **반영**(2026-08-21-auto-update T9, 커밋 03d79dd). `AGENTS.md` Build & Test에 발행 순서 5단계(버전 → 릴리즈 빌드 → 설치 파일 → `certutil` SHA256 → 태그·릴리즈 첨부·본문 기재)를 적었고, **본문에 체크섬이 없으면 앱이 그 릴리즈를 받지 않는다**는 사실도 함께 적었다(FR-62의 대조가 그 값을 기대값으로 쓴다). 같은 자리에 릴리즈 노트 작성 규약(사용자 요청 — 항목당 한두 줄·내부 사정 제외·빈 절 제거)과 베낄 예시를 붙였다.
- [2026-07-29 → 2026-08-22] 설정 팝업 두 항목(`업데이트`·`릴리즈 노트`)의 실제 기능 → **반영**(2026-08-21-auto-update T7, 커밋 c2ce605). 둘 다 비활성을 벗었다 — `업데이트`는 그 자리에서 새 판을 확인하고(설치본에서만 활성), `릴리즈 노트`는 브라우저로 GitHub 릴리즈 목록을 연다. `pending_item`은 쓰임이 사라져 소스에서 지웠다. **이 항목은 처음 대장 스캔에서 「주제가 닿지 않는다」고 잘못 세었고 F-7이 잡았다.**

### 2026-08-20 — 사이트 목록 내보내기·가져오기 (plan `2026-08-20-site-export-import`)

- [2026-08-20 → 2026-08-20] `ui/toast.rs`의 `문구는_인벤토리_원문_그대로다`가 언어를 잠그지 않아 간헐 실패 → **반영**(2026-08-20-site-export-import T4). 대장이 적은 대로 `LanguageGuard::lock` 한 줄로 끝났고 4회 연속 통과로 확인했다. 이번 회차의 Files 밖이었지만 남겨 두면 **이후 모든 task의 「전건 통과」 판정이 흔들려** 그 자리에서 고쳤다

### 2026-08-20 — 화면 확인 (사용자 수동 검증)

- [2026-08-18 → 2026-08-20] 라이선스 화면의 수동 검증 7항목이 미확인 → **반영**(사용자가 직접 확인, 전부 정상). 대화 열림·목록 선택 시 전문 전환·이중 라이선스 두 전문·표준 전문 사유 문구·수동 등재 3건·언어 전환 시 전문 원문 유지·닫기 경로 셋. **자동 캡처로는 끝내 판정할 수 없던 축이다** — egui 앱이라 accesskit이 꺼져 있어 빈 화면만 나온다(그 한계는 남는다)
- [2026-08-19 → 2026-08-20] 정보 화면(FR-58) HUMAN-VERIFY 5항목 → **반영**(사용자 확인, 정상). 아이콘 가장자리 계단·자글거림 없음을 포함한다 — 그 회차 요구의 핵심 축이었다
- [2026-08-19 → 2026-08-20] 앱 이름·아이콘·메뉴 HUMAN-VERIFY 6항목 → **반영**(사용자 확인, 정상). 영어 메뉴 한 줄 서기·팝업 메뉴 모서리 3종·`+` 새 탭 메뉴·언어 전환 시 창 제목과 트레이 툴팁·트레이 복귀 후 작업 표시줄 아이콘


### 2026-08-20 — `ui/app.rs` 책임 분리 (plan `2026-08-20-app-rs-split`)

- [2026-08-15 → 2026-08-20] 같은 이름 확인 흐름을 `ui/transfer_conflict.rs`로 분리 → **반영**(T1). 실제 위치는 **`ui/app/transfer_conflict.rs`**다 — `ExplorerApp`의 필드가 전부 private이라 형제 모듈에서는 접근할 수 없고, **자식 모듈이라야 가시성을 한 곳도 넓히지 않고 나눌 수 있다**(계획 단계에서 별도 crate를 컴파일해 자식→부모·형제↔형제·부모→자식 세 방향을 검증했다). 메서드 6(`apply_drop` 포함) · 상수 1 · 자유 함수 4 · 타입 3 · 시험 7이 옮겨졌다
- [2026-08-15 → 2026-08-20] 규약 시험 둘이 `src/ui` 바로 아래만 훑는다(`read_dir` 비재귀) → **반영**(T3). `ui::dialog`의 `대화는_모두_이_모듈을_거친다`와 `ui::widgets`의 아이콘 리터럴 시험을 재귀로 바꿨고, `dialog.rs`의 자기 제외도 **파일 이름이 아니라 전체 경로 비교**로 고쳤다(재귀에서는 이름 비교가 하위 폴더의 동명 파일을 조용히 빠뜨린다). **재귀화가 실제로 동작함을 임시 파일로 실증했다** — `src/ui/app/`에 금지 아이콘을 둔 파일을 넣자 시험이 잡았다. 이번 회차가 그 폴더를 만들면서 **이 항목이 잠재 결함에서 실제 공백으로 바뀌었기 때문에** 함께 고쳤다


### 2026-08-20 — 요구 제외 (PRD 개정)

- [2026-07-23 → 2026-08-20] FR-14 분할 프리셋 버튼 1/좌우2/상하2/4분할 (Could) → **기각 — 요구 자체를 뺐다**(사용자 결정). 자유 분할(FR-1)이 네 방향 중첩으로 **어떤 레이아웃이든 만들 수 있어** 프리셋은 조작 횟수만 줄이는 편의 기능이었다(4분할을 3회 조작 → 1회 클릭). `docs/prd.md`에서 FR-14를 REMOVED로 내리고 Out of Scope·결정 이력에 사유를 적었다

### 2026-08-20 — 소품 리팩터 묶음 (plan `2026-08-19-small-refactors`)

**반영 6건**

- [2026-08-19 → 2026-08-20] [SUGGEST] `examples/gen_licenses.rs`의 `main` 두 갈래가 `CrateEntry` 필드를 똑같이 채운다 → **반영**(T1). 공통 다섯 필드(`name`·`version`·`spdx`·`authors`·`bundled` — 대장은 넷이라 적었으나 `bundled: false`도 같았다)를 한 번만 적고 `text_indices`·`standard_text`만 분기한다. 동작 무변경은 재생성한 `assets/licenses.json`의 SHA256이 종전과 같은 것으로 실증했다. **이 항목은 2026-08-18·2026-08-19 두 번 등재돼 있어 두 줄 모두 종결한다**
- [2026-08-17 → 2026-08-20] [SUGGEST] `fs::icons`의 잠금을 블록 스코프로 좁힌다 → **반영**(T2). 대장이 적은 둘(`icon_index_for_path`·`shell_display_name`)에 **`icon_index`의 경로별 조회 갈래**를 더해 셋을 고쳤다(대장이 놓친 자리다). 캐시 조회 메서드 다섯 자리가 같은 형태가 됐다. **대장이 적은 반대 근거("여러 지역 변수를 써서 감싸면 오히려 길어진다")는 실측과 달랐다** — `SHFILEINFOW`가 이미 블록 밖에 선언돼 있어 호출식만 감싸면 되고 줄 수는 그대로다. 덧붙여 이 잠금은 **비시험 빌드에서 빈 구조체**라 실행 파일 동작은 바뀌지 않는다(이득은 시험 빌드 잠금 구간 축소와 형태 통일)
- [2026-07-29 → 2026-08-20] 옛 세션 호환 시험의 JSON 문자열 replace → **반영**(T4). `serde_json::Value`에서 키를 재귀로 지우는 `strip_keys`로 바꿔 값 리터럴 의존이 사라졌다(시험 본문에서 `200.0`·`tiles`·`large_icons` 검색 0건). 이름이 같은 `DockSession.columns`도 함께 지워지는 부수 효과는 주석에 적었다
- [2026-08-15 → 2026-08-20] `apply_conflict_choice`의 `Option<ConflictChoice>` 3상태화 → **반영**(T6). `ConflictDecision { NotAsked, Overwrite, Skip }`을 `ui::app` 지역에 두고 `From<ConflictChoice>`로 이었다(대화가 돌려주는 `ConflictChoice`는 `remote_menu`도 쓰므로 그대로 둔다). 옛 네 갈래와 새 세 갈래의 조건·반환이 1:1로 대응함을 리뷰가 대조했다
- [2026-08-15 → 2026-08-20] `theme::PRIMARY_FILL` 개명 → **반영**(T3). `OK_BAR`로 바꿨다(파일 관례가 「상태 접두 + 역할 접미」이고 `OK_DOT`·`OK_TEXT`·`OK_FILL`·`OK_BORDER`가 이미 있다). 값이 같은 `OK_BORDER`와는 합치지 않았다 — 역할이 달라 합치면 한쪽 조정이 다른 쪽을 끌고 간다(그 사유를 doc 주석에 남겼다)
- [2026-08-17 → 2026-08-20] [SUGGEST] `show_remote_node`의 `#[allow(clippy::too_many_arguments)]` → **반영**(T5). `conn`·`cache`·`folder`를 `RemoteRowCtx`로 묶어 인자를 9개 → **7개**로 줄였다(대장은 6개라 적었으나 9−3+1=7이 맞다). clippy 기본 임계가 7이고 `args > 7`에서 발화하므로 `allow` 없이 통과한다

**사실 오류로 무효 1건** — 대장 서술의 전제가 코드와 달랐다

- [2026-08-17 → 2026-08-20] 트리 메뉴의 화면 밖 보정 크기가 어림값이라 「원격 메뉴처럼 실측 상수로」 → **무효**(방향 자체가 성립하지 않는다). 본받으라던 `remote_menu::menu_size()`도 **어림값**이다 — 그 함수 곁의 `FRAME_PAD = 8.0`에 *"메뉴 테두리와 안쪽 여백을 어림한 값"* 이라고 적혀 있다. 둘 다 실측으로 가려면 프레임 테두리·여백을 egui 스타일에서 읽어야 하고 그것은 별도 회차라, 그 형태로 「대기」에 다시 올렸다

**판단상 기각 7건** — 서술은 지금도 참이지만 고치지 않는 편이 낫다

- [2026-07-29 → 2026-08-20] `ui/address_bar.rs`의 `nav_button`이 `theme::TEXT`·16px을 수동 재기술 → **기각**. **서술은 참이다**(`NAV_ICON_PX = 16.0`이 `widgets::DEFAULT_ICON_PX`와 같고 색도 `theme::TEXT`로 같아, 활성 경로는 `icon_button`과 인자까지 동일한 호출이다). 그럼에도 고치지 않는 이유는 대장이 스스로 적은 반대 근거다 — 비활성 경로는 `TEXT_DIM`이라 `styled`가 필요하므로, 활성만 `icon_button`으로 바꾸면 두 경로가 서로 다른 함수를 쓰게 되어 **「활성·비활성의 글꼴 크기가 같다」는 제약이 코드에서 사라진다**(지금 값이 같은 것은 우연이고 한쪽이 바뀌면 조용히 갈린다)
- [2026-07-29 → 2026-08-20] 빈 영역 클릭 처리가 두 렌더 모듈에서 다른 기법 → **기각**. **대비는 지금도 정확하다** — 자세히 보기는 콘텐츠 아래에 남는 사각형을 따로 잡고, 격자는 영역 전체를 잡은 뒤 `select_request`·`action`이 비었을 때만 보는 사후 게이트를 쓴다. 차이가 레이아웃에서 오므로(격자는 항목이 영역을 채워 「아래 빈 공간」이 없다) 헬퍼로 뽑아도 분기가 헬퍼 안으로 옮겨갈 뿐이다
- [2026-07-29 → 2026-08-20] `file_list::show`의 4-튜플 분해 → **기각**. 표제의 「4-튜플」은 이제 맞지 않지만(지금은 `RenderOutcome` 구조체다) 실질인 `DetailsOutcome`·`GridOutcome` 공통화는 여전히 가능하다. 고치지 않는 이유는 **그 사이 차이가 벌어졌기 때문**이다 — 대장은 *"`sort_click` 하나만 다르다"* 고 적었으나 지금은 자세히 보기 전용 필드가 여럿이라 묶으면 격자 쪽에 빈 필드가 생긴다
- [2026-08-15 → 2026-08-20] `dialog::Shell.clicked`가 `usize` 인덱스 → **기각**. 사용처 12곳에 제네릭을 도입해야 해 소품 규모를 넘고, 위키 `feat-dialog-shell`이 *"대화 종류별 타입이나 빌더는 두지 않았다(공통점이 프레임과 푸터뿐이다)"* 를 이미 설계 결정으로 기록했다
- [2026-08-17 → 2026-08-20] [SUGGEST] `ui/tree.rs::menu_row`와 `ui/remote_menu.rs::menu_row` 공용화 → **기각**. **AGENTS.md 공통화 문턱(3회) 미달**(2곳) — 2026-08-19 사용자 결정
- [2026-08-18 → 2026-08-20] [SUGGEST] `TransferQueue::cancel(id)`와 `remove(ids)`의 `retain` 중복 → **기각**. 같은 문턱 미달(2회)이며, 위임으로 바꾸면 단건 경로에 `HashSet` 할당이 붙는다 — 2026-08-19 사용자 결정
- [2026-08-18 → 2026-08-20] [SUGGEST] `ListRow`에 `matches_display_rules` 기본 메서드 → **기각**. 같은 문턱 미달(2곳) — 2026-08-19 사용자 결정

- [2026-07-29 → 2026-08-18] 설정 팝업 네 항목 중 **`오픈소스 라이선스`** → **반영**(2026-08-18, plan `2026-08-18-open-source-licenses.md`). 타이틀바 설정 메뉴의 그 항목이 좌우 2단 고지 화면을 연다 — 구성 요소 158개(대상 155 + 정적 링크·동봉 3)의 이름·버전·SPDX·저작권과 라이선스 전문 93벌. 고지 자료는 `examples/gen_licenses.rs`가 만들어 `assets/licenses.json`으로 커밋되고, 낡으면 `Cargo.lock` 지문 대조 시험이 잡는다. **대기 항목은 남은 셋(업데이트·릴리즈 노트·정보)으로 좁혔다**
- [2026-08-17] **`IconTextures`의 아이콘 변환이 셸 잠금 밖이다** → **반영**(2026-08-17, plan `2026-08-17-icon-texture-lock.md`). `icon_to_image`가 이제 같은 잠금 안에서 돌고, 잠금 소유가 시험에서 **자원 함수**로 옮겨갔다(잡는 곳 7·잡지 않는 곳 4 — 조합 함수와 private 내부를 잠그면 재진입 데드락). 실패 인덱스도 **키당 3회까지 재시도**한다. **다만 등재 당시의 원인 서술은 미검증 가설이었다** — 최대 16스레드 × 200회 부하로도 `icon_to_image` 실패가 재현되지 않았다(T1 실측). 그래도 잠금 정합과 복원력은 그 자체로 값이 있어 그대로 진행했다
- [2026-08-17 → 2026-08-17] [SUGGEST] `ui/tree.rs`의 즐겨찾기 줄과 잎 노드가 거의 같다(세 번째 지점이면 헬퍼로) — 반영 (2026-08-17-tree-icons T2에서 `tree_row`로 뽑았고 즐겨찾기·잎·드라이브·원격 네 계열이 모두 그것을 쓴다)
- [2026-07-29 → 2026-08-05] `ui/address_bar.rs`의 rustfmt 드리프트 — 반영 (2026-08-04-ftp-integration T0에서 `cargo fmt` 1회로 해소, 이후 전 task가 `fmt --check` 무차이)
- [2026-07-23 → 2026-07-26] shell_menu items_menu의 pidls[0] "items 비지 않음" 암묵 계약 — 반영 (doc 주석 + debug_assert, egui 이식 part1 T3)
- [2026-07-23 → 2026-07-23] part2 실행 — 트리·셸 메뉴·감시·세션·성능 — 반영 (part2 T1~T5 완료, v1 완성)
- [2026-07-23 → 2026-07-23] exe/lnk 아이콘 비동기 프리페치 (T5 성능 미달 시 검토) — 기각 (T5 실측 NFR-1~3 전부 통과 — 프리페치 불필요)
- [2026-07-24 → 2026-07-28] 사이드바 항목 가상 스크롤·커스텀 다크 스크롤바 — 반영 (egui 이식 part2 T2 — `ScrollArea`가 스크롤·히트테스트를 자체 처리해 별도 구현이 필요 없어졌다)
- [2026-07-24 → 2026-07-28] 인라인 이름 편집 EDIT의 다크 스타일링 — 반영 (egui 이식 part2 T2 — Win32 EDIT 대신 `TextEdit`을 쓰면서 밝은 배경 제약이 사라졌다)
- [2026-07-26 → 2026-07-28] 전역 공유 자원 묶기(`SharedResources`) — 기각 (part2 T3에서 재평가: 명령을 `ExplorerApp`이 직접 처리해 `PanelState::show`·`splitter::show_layout`의 인자가 늘지 않았다. 묶어도 호출부 표기만 줄고 내부에서 다시 분해해야 해 실익 없음)
