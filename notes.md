# 작업 내역

## 최근 변경
- 2026-08-05: **연결별 탭 줄이 안 보이던 문제 + 도크 아이콘 두부 (사용자 보고)**
  - 탭 줄은 원본대로 **큐에 든 항목**에서 사이트 이름을 모았다 — 그래서 연결만 하고 아직 옮긴 것이 없으면 탭이 하나도 없었다. 원본 스크린샷은 전송이 도는 상태라 이 구멍이 보이지 않았다. **정적 자산(HTML·스크린샷)은 "그 순간의 상태"만 보여 준다** — 빈 상태·초기 상태는 따로 생각해야 한다.
  - 직전 작업(T35)에서 연결 탭을 위쪽 스트립에 붙였는데, 기준 화면은 **별도의 두 번째 줄**이었다. 사용자가 이미지로 짚어 줘서 알았다 — 디자인 대조를 코드로만 하면 이런 배치 차이는 드러나지 않는다(F-8 화면 확인이 필요한 이유).
  - **두부 3종**: `⏸`·`✕`·`⧉`가 이 앱 글꼴(맑은 고딕+phosphor)에 없어 `?`로 그려지고 있었다. 같은 함정을 메뉴 화살표(`⏵`)에서 이미 겪었는데도 도크 아이콘에서 되풀이됐다 — 회귀 테스트를 "글리프 비교"가 아니라 **"아이콘 글꼴의 사용자 영역 부호점인가"** 로 바꿔 다음 번을 막았다.
- 2026-08-05: **서버 로그에 연결별 탭과 진행 상태 줄 추가 (사용자 요청)**
  - 로그가 어느 연결의 것인지 화면에 없다는 것은 T20 리뷰가 이미 지적해 Deferred에 있던 항목인데, 실사용에서 같은 불편이 확인돼 이번에 닫았다. 큐의 `성공`·`실패` 탭과 **같은 모양**으로 연결 탭을 세워 눈이 옮겨 다니지 않게 했다.
  - 진행 상태 줄(`상태: …에 연결…` → `연결 수립…` → 암호화 여부 → `로그인…` → 목록 조회와 결과)은 실패 지점을 로그만으로 짚기 위한 것이다. 평문으로 떨어진 연결은 그 사실이 로그에도 남는다(상태 표시줄의 `· TLS` 판정과 같은 근거를 쓴다).
  - **교훈**: 연결 테스트 셋이 이벤트 **개수**로 기다리고 있어 로그 한 줄을 더하자 셋이 한꺼번에 깨졌다. "몇 개 올 것"이 아니라 "무엇이 올 것"으로 기다려야 한다 — `wait_for`로 바꾸고 나니 이후 로그 추가에 영향받지 않는다.
- 2026-08-05: **실사용에서 드러난 원격 결함 3건 수정 (폴더 이동·한글 이름·사이트 저장)**
  - **경위**: 실제 FTP 서버에 붙여 본 사용자가 한 번에 세 가지를 알려 줬다 — 한글 폴더명이 `?? ???`로 깨짐, 폴더를 더블클릭해도 안 들어감, 재시작하면 연결 목록이 빔.
  - **폴더 더블클릭**: 여는 경로가 로컬 경로를 요구해 원격 탭에서는 **아예 빠져나갔다**. 주석은 "원격은 다른 task가 맡는다"고 적어 두었는데 그 task가 실제로 배선하지 않았고, plan의 acceptance에도 없어 리뷰 4라운드가 전부 통과시켰다. **"다른 곳에서 한다"는 주석은 그 자체로는 아무것도 보장하지 않는다.**
  - **한글 이름**: `suppaftp`가 목록 줄을 UTF-8로 단정해 디코딩한 뒤 넘겨줘 CP949 바이트가 이미 뭉개진 상태였다. 그동안 "라이브러리 제약"으로 Deferred에 두었는데, 다시 읽어 보니 `custom_data_command`가 **데이터 연결을 그대로 내준다** — LIST를 직접 보내 원문 바이트를 받으면 우리가 옮길 수 있다. 제약이라고 적어 둔 것을 한 번 더 확인해 볼 값어치가 있었다(보내는 쪽은 여전히 막혀 있어 그 한계는 Deferred에 남겼다).
  - **사이트 저장**: 저장·복원 코드는 멀쩡했고 원인은 **앱이 죽어 `on_exit`가 돌지 못한 것**이었다(같은 날 고친 크래시). 그래도 종료에만 기대지 않도록 사이트가 바뀌는 즉시 세션을 적게 했다 — 비정상 종료가 다시 나도 목록은 남는다.
  - **자동화 주의**: GUI 자동화에서 `SendKeys`를 썼다가 초점이 다른 창으로 새어 **작업 중인 터미널에 글자가 들어갔다**. 이후로는 클릭만 쓰고, 입력이 필요한 값은 세션 파일에 미리 심는 방식으로 바꿨다.
- 2026-08-05: **사이트 연결 시 앱이 죽던 결함 수정 (FTP 통합 후속)**
  - **증상**: 사이트 관리자의 `연결(C)`이나 연결 목록 더블클릭 — 두 경로 모두에서 앱이 그대로 종료됐다. 사용자가 실행해 보고 알려 줬다.
  - **원인은 두 기능이 만나는 자리에 있었다**: 연결은 활성 패널을 좌우로 나눠 새 패널을 만드는데, 그 패널은 태어나자마자 **시작 폴더를 읽기 시작**한다. 곧이어 활성 탭이 원격으로 바뀌고, 뒤늦게 도착한 로컬 열거 결과가 원격 탭에 커밋되려다 단언에 걸려 죽었다(`TabState::set_committed`). **배포 빌드는 더 나쁘다** — 단언이 빠져 죽지 않는 대신 원격 탭이 조용히 로컬 탭으로 둔갑한다.
  - **고친 방식**: 원격 탭을 열 때 진행 중인 로컬 열거를 접고(세대를 올려 이미 채널에 실린 답도 버린다), 그래도 도착한 결과는 활성 탭이 로컬일 때만 반영한다. "늦게 온 답은 버린다"는 기존 규율에 **"가는 곳이 사라진 답"** 을 더한 것이다.
  - **교훈(3회째 같은 형태)**: 단위 테스트가 각 기능의 안쪽만 보고 **두 기능이 만나는 순서**를 보지 않으면, 빌드·clippy·리뷰 4라운드를 통과해도 첫 실행에서 죽는다. 이번에도 실행해 본 것은 사용자였다. 재현은 디버그 exe를 stderr 리다이렉트로 띄우고 자동 클릭을 넣어 백트레이스를 얻는 방식이 즉효였다 — GUI 앱이라 콘솔이 없어 평소에는 패닉 메시지가 어디에도 남지 않는다.
- 2026-08-05: **FTP·FTPS·SFTP 원격 연결 (T0~T26, plan `docs/plans/2026-08-04-ftp-integration.md`)**
  - **경위**: PRD에 FR-27~46·NFR-10~13을 더하고 27개 task로 나눠 자율 루프로 진행했다. 각 task마다 사양·품질 리뷰 두 벌을 받았고, 그 지적으로 잡은 것 중 몇은 화면에서만 드러났을 결함이었다(도크가 패널 그리드를 0 높이로 밀어낸 것, 대화의 취소 단추가 아무 일도 하지 않던 것, 트리에서 폴더를 골라도 목록이 다시 읽히지 않던 것).
  - **기각한 대안 ① async 런타임**: `suppaftp`·`ssh2` 둘 다 동기 API고 GUI는 이미 메시지 루프를 돈다. tokio를 얹으면 런타임 하나를 더 이고 가면서도 실제로 얻는 것은 "연결마다 스레드 하나"뿐이라, **연결당 워커 스레드 + 채널**로 갔다(AGENTS 동시성 규약과도 같은 결론).
  - **기각한 대안 ② 비밀번호 평문 저장**: 설정 파일이 곧 백업·동기화 대상이라 평문은 곧 유출이다. DPAPI로 봉인해 담고, **봉인에 실패하면 담지 않는다**(평문으로 대신하지 않는다). 다른 PC에서는 풀리지 않아 다시 입력받는다 — 그것이 옳은 실패다.
  - **기각한 대안 ③ 디자인의 자동 분할(기존 쌍 접기)**: 기준 화면은 연결하면 **기존 패널 쌍을 접고** 화면을 새로 둘로 가르는 모습이었으나, 그러면 사용자가 짠 배치가 말없이 사라진다. 대신 **활성 패널을 좌우로 나눠 오른쪽에 원격 탭을 여는** 쪽을 택했다(FR-35·D16). 나눌 공간이 없을 때만 현재 패널에 연다.
  - **기각한 대안 ④ 외부 테스트 서버 의존**: 실서버에 붙는 테스트는 네트워크·계정·요금에 묶여 CI도 재현도 안 된다. 가짜 서버·세션(`remote::testing`)이 지연·무응답·거절·대량 목록·`SITE CHMOD` 미지원까지 흉내 내며, 실서버 확인은 환경변수(`FE_TEST_FTP_URL`·`FE_TEST_SFTP_URL`)로 수동으로만 한다(AGENTS에 사용법 기록, 실제 주소·계정은 적지 않는다).
  - **NFR 실측(release, 같은 PC·3회 중앙값)**: 시작 94ms(추가 전 114ms) · 유휴 127.9MB(추가 전 129.6MB)로 **NFR-1·2 회귀 없음** — 재시작 시 자동 재연결을 하지 않아 시작 경로가 늘지 않는다. 1GB 동시 4건 전송 중 작업 집합 +396KB(임계 50MB) · 1만 항목 원격 폴더 조회 왕복 4ms · exe 5.06MB → 7.14MB(+2.09MB, `suppaftp`·`ssh2`와 TLS 의존성 몫). NFR-10은 `suppaftp::`·`ssh2::` 참조가 `src/ui/`에 0건임을 grep으로 확인했다.
  - **exe 증가분 판단**: plan Deferred의 "`remote::testing`이 릴리스 빌드에 포함된다"는 이번 실측으로 판정했다 — 증가분의 대부분은 프로토콜 crate와 그 암호 의존성이고 가짜 서버는 수십 KB 수준이다. feature 게이트를 새로 들이는 값이 더 커 **그대로 둔다**.
  - **미처리(plan Deferred)**: 사이트별 문자셋이 목록 디코딩까지 닿지 않는다(라이브러리가 이미 손실 디코딩을 해 버린다 — `remote::charset` 모듈 doc에 경위) / 원격 조회 요청의 세대 번호 공간을 상수(`1<<40`)로 나눈 것 / 패널 목록 조회 실패를 화면에 잇기 / `ui/app.rs`·`ui/panel.rs`가 분리 검토선을 넘었다(각각 2400·2100줄) / 디자인 HTML의 샘플 호스트가 실제 IP 형태라 **push 전에 가릴지 사용자 판단이 필요하다**.
- 2026-08-04: **타이틀바 아래 구분선 추가** — 참조 화면에 있는 가로선이 이 앱에는 없어 요청받았다. egui `Panel`의 `show_separator_line`을 켜지 않고 `titlebar`에서 `painter().hline`으로 직접 그렸다 — 기본 구분선 색은 전역 `visuals`라 다른 패널 구분선과 함께 움직이므로 이 선만 조정할 수 없다. 색은 앱 팔레트의 `theme::PANE_BORDER`(0x333333) 재사용. 빌드·clippy·테스트 통과, 화면은 사용자 확인 필요.
- 2026-07-31: **최대화/이전 크기 전환 시 화면 떨림 수정 — 원인이 둘이었다 (커스텀 타이틀바)**
  - **원인 하나를 고치고 완료 보고했다가 되돌아왔다**: 더블클릭 경로의 결함(아래 ②)을 찾아 고치고 "버튼 클릭 경로는 계측상 정상"이라고 보고했는데, 사용자가 **버튼 클릭에서도 떨린다**고 알려 와 조사를 다시 열었다. 첫 조사에서 창 rect·egui 레이아웃만 계측하고 **실제 화면 픽셀을 보지 않은 것**이 놓친 이유다 — 둘 다 정상이어도 화면은 떨릴 수 있었다(합성은 앱 밖에서 일어난다). 전환 직후 화면을 연속 촬영하고 나서야 원인 ①이 드러났다.
  - **① DWM 전환 애니메이션이 옛 화면과 새 화면을 겹친다 (버튼 클릭·더블클릭 공통, 진짜 주범)**: Windows가 최대화·복원에 넣는 교차 페이드는 **바뀌기 전 창의 스냅샷**과 새 화면을 겹쳐 보여준다. 창 크기에 따라 패널·목록이 재배치되는 이 앱에서는 두 화면의 글자 위치가 크게 달라, 겹치는 동안 글자가 이중으로 보이며 떨리는 것처럼 읽힌다. 게다가 애니메이션이 도는 동안 새 화면을 내보내는 시각이 밀려(전환 직후 프레임 간격 300ms대) 겹침이 오래 남았다. **앱의 UI 구성 시간은 내내 1ms 미만**이었다 — 앱이 느린 게 아니라 합성 대기였다. `DWMWA_TRANSITIONS_FORCEDISABLED`로 이 창의 전환 애니메이션을 끄니 겹침이 사라지고 전환 직후 간격도 50~90ms대로 내려갔다(캡처·프레임 로그로 확인).
  - **대가**: 최대화·복원·최소화가 애니메이션 없이 곧바로 바뀐다. 창 장식을 앱이 직접 그리는 이 앱에서는 OS 애니메이션과 어차피 어울리지 않아 받아들였다. 되돌리려면 `theme::disable_window_transitions` 호출 한 줄을 빼면 된다.
  - **② 타이틀바 더블클릭이 창을 끌고 있었다 (더블클릭 경로)**: 아래 상세. ①과 별개 결함이라 함께 고쳤다.
  - **계측이 원인을 갈랐다**: 창 rect 샘플링(진동 없음) → 프레임 단위 프로브(중간 크기 없음) → CPU 구성/유휴 시간 분리(앱 1ms, 유휴 300ms) → 전환 직후 연속 캡처(겹침 확인). 앞 셋만 봤을 때 "앱은 정상"이라는 결론이 나왔고, 그건 맞았지만 **문제가 없다는 뜻은 아니었다**.
  - **원인**: 빈 영역 끌기 판정이 `is_pointer_button_down_on()`이라 **누른 모든 프레임**에 참이었다. 그래서 단순 클릭·더블클릭에서도 `StartDrag`가 나가 OS 창 이동 루프가 열렸고, 손이 미세하게 흔들린 만큼 창이 실제로 따라 움직인 뒤 최대화 토글이 겹쳐 화면이 떨리며 바뀌었다. 프레임 단위 프로브로 더블클릭 1회에 `Drag → Drag → 창 이동(3.3, 2.0) → Drag → ToggleMaximize` 순서를 직접 관측해 확정했다.
  - **왜 기존 주석(D4)이 못 막았나**: "더블클릭한 **프레임**에는 끌기를 요청하지 않는다"로 같은 프레임 충돌만 배제했는데, 실제 문제는 더블클릭에 **이르는 앞선 프레임들**이었다. 프레임 단위 배제로는 여러 프레임에 걸친 조작을 못 가린다.
  - **수정**: `drag_started()`로 교체 — egui는 클릭도 받는 위젯에서 포인터가 `max_click_dist`(6px) 이상 움직이거나 `max_click_duration`(0.8초) 이상 눌렸을 때만 끌기로 판정한다. 시작 프레임 1회만 요청하고 이후 창 이동은 OS 루프가 맡는다. 실제 앱에서 끌기 보존(0,0 → 50,33 이동)과 떨림 소멸을 모두 재확인했다.
  - **UI 버그에도 회귀 테스트가 가능했다**: 이 프로젝트가 이미 쓰던 헤드리스 egui(`ctx.run_ui`)에 포인터 이벤트를 프레임 단위로 주입해 클릭/더블클릭/끌기 3종을 고정했다. HWND 없이도 창 조작 판정은 테스트 대상이 된다.
  - **미처리**: `src/ui/address_bar.rs:129`에 이번 변경과 무관한 `cargo fmt` 위반이 남아 있다(범위 밖이라 손대지 않음).
- 2026-07-30: **F-8 화면 확인 — 결함 2건 발견·수정 (썸네일 미표시, 하위 메뉴 화살표 두부)**
  - **경위**: 사용자가 "직접 확인해줘"라고 해, F-8의 화면 확인 13항목을 사용자 대신 수행했다. DPI 인식 스레드에서 창을 물리 좌표로 조작·캡처하는 스크립트로 클릭·호버·드래그·스크롤까지 실제로 넣어 확인했다. 13항목 전부 통과했고, 그 과정에서 자동 검증이 놓친 결함 2건이 드러났다.
  - **썸네일은 만들어지는데 화면에 없었다**: 픽셀 생성(`GetImage`)·LRU 캐시·텍스처 업로드가 전부 정상이었고 빠진 것은 **다시 그리라는 신호**였다. 열거(`DirLoad`)는 워커가 `ctx`를 들고 있어 스스로 깨우지만, 썸네일 워커는 `fs` 계층이라 egui를 모른다(의존 단방향) — egui는 입력이 없으면 프레임을 돌리지 않으므로 마우스를 움직일 때까지 형식 아이콘에 머물렀다. `poll_thumbnails`가 "다시 그릴 지연"을 값으로 돌려주고 호출부가 `request_repaint_after`를 부르게 했다(대기 중 50ms 간격 자기 기동).
  - **첫 시도의 테스트가 무효였다**: `ctx.has_requested_repaint()`로 검사했더니 **수정을 제거해도 통과**했다 — `load_texture`가 스스로 repaint를 일으켜 둘이 구분되지 않았다. 판정을 반환값으로 분리하고 나서야 회귀를 잡았다(제거 실험으로 확인). 부작용을 관찰하는 테스트는 그 부작용의 다른 출처를 배제해야 유효하다.
  - **`?` 문자가 메뉴에서 재발했다**: `ui.menu_button`이 붙이는 egui 기본 화살표가 `⏵`(U+23F5)인데, 이 앱은 egui 내장 글꼴을 끄고 맑은 고딕만 쓰므로 글리프가 없어 두부가 됐다. 사용자 요구 5번(사이드바 `?` 제거)과 같은 성질의 문제가 다른 자리에서 나온 것 — 아이콘 글꼴(phosphor)의 `CARET_RIGHT`로 교체했다. **egui가 기본 제공하는 문자열 아이콘은 이 앱에서 항상 두부 후보다.**
  - **반복 패턴 3회째**: 직전 작업의 notes에 적은 *"단위 테스트는 통과하지만 실제 호출 경로가 그 로직을 지나지 않는다"*가 또 나왔다. 이번에는 "만들고 올리는" 단계마다 테스트가 있었으나 **"그 결과가 화면에 도달하는" 단계**에 테스트가 없었다. 워커 → 캐시 → 텍스처 → 화면 중 마지막 계단이 비어 있었던 것.
  - **DPI 함정**: DPI 비인식 프로세스로 창을 재면 1650×1050 창이 1100×700으로 보이고 그 영역만 캡처된다 — 열·항목 수·메뉴 버튼이 잘린 것처럼 보여 **없는 결함을 만들어낸다**. `SetThreadDpiAwarenessContext(-4)` 없이 본 화면은 근거로 쓰지 않는다.
  - **NFR 실측**: 패널 2개 유휴 128.8MB(기준 150MB 미만) · 10만 파일 폴더에서 모드 전환 2.5~2.8초·응답 유지 · 260장 사진 폴더 전 구간 스크롤 후 +16.7MB(전량 유지 시 예상 65MB보다 작아 축출 동작).
  - **콜드 스타트: 위반으로 보고했다가 뒤집혔다 (최종 0.4~0.6초, 기준 1초 충족)**: 조사 초반 4.5~10초가 반복 측정돼 NFR-1 위반으로 판정하고 문서·커밋·push까지 했다. 그런데 `log` 로거로 eframe 내부를 들여다보는 단계에서 **같은 조건이 0.3~0.7초**로 나왔고, 이후 순정 릴리즈·빌드 직후 즉시·exe 새 복사본·간격 3~60초 어느 조합에서도 24회 이상 전부 0.3~0.7초였다. 느린 수치는 재현되지 않는다.
  - **틀린 판정을 공개 저장소에 push한 뒤에 알았다**: "여러 번 반복 측정했으니 확실하다"가 근거였는데, 그 반복이 전부 **같은 시간대·같은 시스템 상태**에서 이뤄졌다는 점을 보지 못했다. 반복 횟수는 표본의 독립성을 대체하지 못한다 — 조건을 바꿔가며 재현되는지가 판정 기준이어야 했다.
  - **초기화 구간 기준선을 얻었다** (`log` 로거 실측, 여유를 아는 값): `main`~`run_native` 4~14ms / winit 루프 진입 5~16ms / **glutin Display 생성(WGL 픽셀 포맷 열거) 260~420ms — 대부분** / GL 컨텍스트·painter 약 50ms / 폰트·아이콘 약 36ms. 더 줄일 곳은 Display 생성뿐이고 그건 드라이버 영역이라 앱 코드로 못 건드린다.
  - **끊긴 네트워크 드라이브는 범인이 아니었다**: 연결이 끊긴 네트워크 드라이브(`Z:`)에 접근하면 8.8초가 걸려 콜드 스타트와 **수치가 거의 일치**했다. 유력해 보였지만 PATH에 `Z:`가 없고 시작 폴더도 홈이며 트리는 `GetLogicalDrives`(66ms)만 쓴다. **수치 일치는 증거가 아니다** — 계측으로 구간을 가르기 전까지는 이 가설을 확정으로 취급하지 않았다.
  - **창 옵션·실행 간격은 원인이 아니었다**: 환경변수로 조합을 고르는 계측 빌드 하나를 만들어 `full`·`bare`(옵션 전부 기본)·`decor`(장식 켬)·`nodark`를 각 3회 측정했다. 그런데 **같은 `full` 설정이 연속 실행에서 1.2~2.0초, 대기 20초에서 4.5~9.2초**로 정반대로 나왔다 — 조합별 수치를 원인으로 읽으면 틀린 결론이 된다. 값 자체가 1.2~10.3초로 8배 흔들리므로 시스템·드라이버 상태 의존으로 보인다. **가장 빠를 때조차 1.2초로 기준(1초) 미달이다.**
  - **측정이 흔들릴 때는 조합 비교가 무의미하다**: 처음 한 바퀴에서 `full`이 가장 빨라 "현행 설정이 최적"으로 읽힐 뻔했으나, 같은 설정을 조건만 바꿔 재측정하니 재현되지 않았다. n=3에 8배 변동이면 조합 간 차이는 잡히지 않는다 — 변동 폭을 먼저 확인해야 비교 설계가 성립한다.
  - **배제된 가설**: 창 옵션 4조합 · 실행 간격 3~60초 · 끊긴 네트워크 드라이브 · Defender 첫 스캔(새 복사본 첫 실행 663ms vs 이후 356ms로 영향 미미) · 빌드 직후 상태. 전부 측정으로 걸렀고, 느린 수치의 정체는 끝내 특정하지 못했다 — 그 시점의 일시적 시스템 부하로 보고 Deferred에 기준선과 함께 남겼다(재발 시 대조용). 계측 패치는 두 번 다 커밋하지 않았다.
- 2026-07-29: **보기 모드 8종·패널 메뉴·썸네일·목록 표시 수정 (T1~T14)**
  - **경위**: 사용자가 실행 화면을 보고 9가지를 한 번에 지적했다 — 분할 메뉴 확장, 가로 스크롤 없음, 열 폭 조절 불가, 이름 겹침, 사이드바 `?`, 상단 메뉴 삭제, 항목 수 표시, 보기 모드 8종 검토, 이름 말줄임. 표시 결함 수정(T1~T5)·진입점 재편(T6~T7)·보기 모드 확장(T8~T14) 세 덩어리로 나눠 진행했다.
  - **PRD를 먼저 고쳤다**: 요청이 FR-1(분할 진입점에 "메뉴 바" 명시)·FR-4(상세 보기만)·FR-16·FR-21과 정면으로 어긋나, 코드보다 PRD를 먼저 갱신하고(FR-23~26·NFR-9 신설) 승인을 받았다. 그러지 않으면 Phase G 재검증의 기준 자체가 틀린 채로 통과한다.
  - **이름 겹침과 말줄임은 같은 원인이었다**: `Painter::layout`의 폭 인자가 자르는 폭이 아니라 **줄바꿈 폭**이라 긴 이름이 2줄이 되고, 행 높이가 20px 고정이라 아래 행과 포개졌다. 주석은 "잘라 그린다"고 적혀 있어 코드와 어긋나 있었다. `TextWrapping { max_rows: 1, overflow_character: '…' }`가 둘을 함께 해결한다.
  - **마지막 열이 "남는 폭 전부"인 한 가로 스크롤은 불가능했다**: 콘텐츠 폭이 뷰포트에 의존하면 스크롤 계산과 순환한다. 네 열 모두 고정 폭으로 바꾸고, 합이 뷰포트보다 좁을 때만 마지막 열을 늘려 빈틈을 없앴다(표시만 — 저장 폭은 그대로라 창 크기를 바꿔도 사용자가 정한 폭이 안 덮인다).
  - **메뉴 바를 지워도 잃는 기능이 없었다**: 16개 항목 중 15개는 다른 진입점(주소창·탭 스트립·사이드바 `+`·컨텍스트 메뉴)에 이미 있었고, 유일한 예외인 '패널 닫기'를 패널 메뉴로 옮겼다. 생성처가 사라진 `Command` variant 3개와 고아가 된 private 메서드 3개도 함께 걷어냈다.
  - **패널 메뉴 명령은 활성 패널이 아니라 "메뉴를 연 패널"에 적용한다(D16)**: 활성 판정이 포인터 위치 기반이라, 팝업이 옆 패널 위로 뻗으면 그 아래 패널이 활성이 된다 — 그대로 두면 '닫기'가 엉뚱한 패널을 닫는다. 기존 분할이 `PanelId`를 실어 올리던 이유와 같아 그 계단을 그대로 이었다.
  - **세션 스키마는 버전을 올리지 않았다(D5)**: `parse_session`이 버전 불일치 시 통째로 폴백해 기존 사용자의 워크스페이스·분할·탭이 초기화된다. `#[serde(default)]` 필드만 더했고, 그 대가로 **필드 나열 리터럴 6곳**(테스트 픽스처 4곳 포함)이 깨져 `cargo build`만으로는 드러나지 않았다 — `Default` derive로 정리했다.
  - **아이콘 인덱스는 크기와 무관하게 같은 체계다**: 그래서 텍스처 캐시가 인덱스만 키로 쓰면 16px 텍스처가 256px 자리에 그대로 나온다. `(이미지 리스트, 인덱스)` 복합키로 고쳤다(T9). 96px 단계는 시스템에 없어 256px를 줄여 쓴다.
  - **썸네일에서 리뷰가 잡은 세 결함**: ① 프레임당 업로드 상한(4)에 걸린 경로가 `poll()`에 다시 안 나와 **영구히 형식 아이콘으로 남았다** ② 픽셀은 LRU로 축출되는데 텍스처는 폴더 전환 때만 비워져 GPU 메모리가 무제한 늘었다 ③ 그리기가 텍스처만 봐서 LRU `touch`가 한 번도 안 불렸다(보고 있는 썸네일이 축출됨). 셋 다 "방금 도착한 것만 본다"가 뿌리라, 매 프레임 픽셀 캐시 전체와 맞추는 `sync`로 한 번에 고쳤다.
  - **알파 처리**: 썸네일에서 알파 0인 픽셀을 개별적으로 불투명화하면 **진짜 투명한 PNG의 가장자리가 검게 메워진다**. 같은 저장소의 `icon_tex`가 쓰던 "전체가 0일 때만" 판정으로 맞췄다.
  - **메모리 실측(NFR-9)**: 장당 262,144 bytes(256×256×4) × 상한 200장 = **50.0MB**. Working Set은 할당자·GPU 드라이버·egui 아틀라스가 섞여 썸네일 몫만 분리되지 않아, 캐시가 실제로 쥔 픽셀 바이트를 재는 방식으로 바꾸고 테스트로 고정했다.
  - **검증 한계**: 데스크톱 GUI라 자율 검증은 소스 대조·단위 테스트까지다. 실제 화면(보기 모드 8종의 생김새, 썸네일이 뜨는 모습, 열 드래그 감촉, 메뉴 호버 동작)은 F-8 사용자 확인이 정본이다.
  - **Deferred**: 자세히 보기의 고정 헤더(가로 스크롤을 위해 헤더를 본문과 같은 스크롤에 넣어 세로 스크롤 시 함께 올라감) · 보기 모드별 정렬 기준 기억 · 열 추가/제거/순서 변경 · `list_grid`의 `visible` 부수 출력 구조 · `from_tabs` 인자 증가 · 옛 세션 호환 테스트의 JSON replace 방식

## 아카이브 인덱스
- 2026-07.md: Win32 판 v1 완성(분할·탭·트리·셸 메뉴·감시·세션), 워크스페이스 사이드 패널, 탐색기 전체 다크 테마, 목록 헤더 다크 수정, egui(eframe) 이식 PoC·part1·part2, 네 방향 분할, 커스텀 타이틀바, 탭 스트립 통합·분할 패널 테두리, 스크롤 영역 ID 충돌 수정
