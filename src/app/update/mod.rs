//! 자동 업데이트 (FR-62) — GitHub 릴리즈에서 새 판을 찾아 받아 설치한다.
//!
//! 네 갈래로 나눈다: `http`(WinHTTP GET)·`sha256`(무결성 대조용 해시)·
//! `release`(릴리즈 조회·버전 비교)·`install`(설치본 판정·내려받기·설치 실행).
//! 그 위에 이 파일이 상태와 워커를 얹는다.
//!
//! `ui`를 모른다 — 화면은 여기가 내놓는 상태를 읽어 그릴 뿐이다(계층 단방향).
pub mod http;
pub mod release;
pub mod sha256;
