# 2026-07 작업 기록 (아카이브)

- 2026-07-25: **파일 목록 컬럼 헤더 다크 미적용 수정 (다크 후속 3차)**
  - **무엇을**: `file_list.rs`의 헤더 커스텀드로우를 "글자색만 지정 + `CDRF_DODEFAULT`"에서 **완전 직접 그리기 + `CDRF_SKIPDEFAULT`**로 전환(`draw_header_item`이 항목 배경·구분선·제목을 그림). 열이 없는 오른쪽 여백은 항목이 아니라 헤더 기본 도색이 그리므로 `CDDS_POSTPAINT`에서 `fill_header_gap`으로 덮는다(PREPAINT에서 채우면 기본 도색이 다시 덮어 무효). 효과 없던 `SetWindowTheme(header,"ItemsView")` 제거.
  - **왜**: 헤더 글자는 다크인데 배경만 흰색으로 남는다는 사용자 재보고. 원인은 **시스템 테마(ItemsView + AllowDarkModeForWindow)로 헤더 배경을 다크로 그리게 하는 방식이 이 환경에서 동작하지 않는 것** — 기본 도색이 밝은 배경을 그린다. 통지 도달 경로(ListView 서브클래스) 자체는 정상이었다. 같은 이슈를 3차까지 끌었던 이유는 1·2차 모두 "코드 확인"만으로 완료 처리하고 시각 검증을 미뤘기 때문 → 이번엔 앱 실행·창 캡처로 실측 확인.
  - **핵심 결정**: 헤더도 탭·메뉴·버튼과 마찬가지로 **시스템 테마에 기대지 않고 직접 그린다**(이 앱의 다크는 표준 컨트롤=색상 메시지, 오너드로우 계열=직접 그리기로 이원화). 정렬 화살표는 현재 열에 설정하지 않으므로 그리지 않으며, 추후 정렬 표시를 넣으면 SKIPDEFAULT 때문에 직접 그려야 한다.
  - **검증 결과**: cargo build/clippy(-D warnings)/fmt/test(75+2) 통과 + 실행 화면 캡처로 헤더 다크 확인.
  - **변경 파일**: src/panel/file_list.rs, debug-2026-07-24-dark-ownerdraw.md(Phase 6)
- 2026-07-24: **탐색기 전체 다크 테마 + 사이드바 크기 조절 잔상 개선 (T1~T7)**
  - **무엇을**: ① T1 사이드바 크기 조절 잔상 제거 — `LayoutHost::defer_into`/`pane_count` 추가로 `layout_children`이 사이드바+탐색기 패널을 **단일 `DeferWindowPos` 배치**로 묶어 시차 제거, 사이드바 `paint`에 **메모리 DC 더블버퍼링**(BitBlt 1회) 적용. ② T2 다크 인프라 `app/theme.rs` 신규(공용 다크 색상 + `apply_dark_titlebar`) + 메인 창 배경 브러시 다크(`CreateSolidBrush`) + DWM 다크 타이틀바(`Win32_Graphics_Dwm` feature 추가). ③ T3 파일 목록(ListView) 다크 — `SetWindowTheme("DarkMode_Explorer")` + `LVM_SETBKCOLOR/TEXTCOLOR/TEXTBKCOLOR` + ListView 서브클래스(`list_dark_proc`)로 헤더 `NM_CUSTOMDRAW` 다크. ④ T4 폴더 트리(TreeView) 다크 — `TVM_SETBKCOLOR/TEXTCOLOR/LINECOLOR`. ⑤ T5 주소창 Edit(`WM_CTLCOLOREDIT`) + 네비 버튼 3개(`BS_OWNERDRAW` + `draw_nav_button` 오너드로우) + 패널 컨테이너 배경·상태 라벨(`WM_CTLCOLORSTATIC`) 다크, `PanelState`에 브러시 필드 + Drop 해제. ⑥ T6 탭 컨트롤(`TCS_OWNERDRAWFIXED` + `draw_tab`, `WM_DRAWITEM` ODT_TAB 분기) 다크. ⑦ T7 메뉴바 오너드로우 다크 — menu.rs를 `MENUITEMINFOW`/`InsertMenuItemW`(`MFT_OWNERDRAW`)로 재작성 + window.rs `WM_MEASUREITEM`/`WM_DRAWITEM`(ODT_MENU) + `measure_menu_item`/`draw_menu_item`/`menu_font`/`split_accel`.
  - **왜**: 사용자 요청 — 사이즈 조절 시 잔상/부드럽지 않음 + 탐색기 화면 전체 다크. PRD Out of Scope였던 "앱 전체 다크 모드"를 *테마 전환 UI 제외*로 한정하고 고정 다크(FR-21 신규)로 채택. 별도 처리: 워크스페이스 패널 폰트 확대(이름 13→15·부제 11→13·헤더 12→14)와 크기 조절 흰 깜빡임(메인 창 `WS_CLIPCHILDREN`)은 선행 커밋으로 완료. plan: docs/plans/2026-07-24-dark-theme-and-resize.md
  - **핵심 결정**: ① 다크 팔레트를 `app/theme.rs` 공용 모듈에 두되 각 색은 실제 쓰는 task에서 추가(YAGNI), 값은 사이드바 사설 팔레트와 통일(사이드바 리팩토링은 범위 외) ② 표준 시스템 컨트롤은 `SetWindowTheme`+색상 메시지로, 헤더·버튼·탭·메뉴는 오너드로우 직접 그리기(테마 다크가 안 먹음) ③ 잔상은 배치 통합(단일 defer) + 더블버퍼링이 근본 — 두 영역 시차·순차 그리기 제거 ④ 패널 배경·상태라벨을 T5에 포함(주소창이 얹히는 컨테이너가 밝으면 미완성) ⑤ 메뉴바 팝업 배경은 Win32 제약상 best-effort(표준 API로 완전 다크 불가) ⑥ `register_class`는 첫 등록만 배경 브러시를 클래스에 연결하고 중복 등록 시 즉시 해제(패널 다중 생성 시 GDI 누수 방지 — T5 리뷰 M1)
  - **검증 결과**: 각 task cargo build/clippy(-D warnings)/test(75→76, split_accel 추가) 통과 + 전체 fmt --check 통과. task마다 spec+quality 이중 리뷰(T5 quality M1 브러시 누수·T7 spec MAJOR 테스트 누락 발견 → 수정 후 재리뷰 통과). **다크 렌더링·잔상 소멸은 빌드로 검증 불가 — 화면 동작은 HUMAN-VERIFY 잔여**(특히 메뉴바 팝업 배경 best-effort, 목록 헤더 다크, 각 컨트롤 색 대비).
  - **변경 파일**: src/app/{theme}.rs(신규), src/app/{window,menu,mod,layout_host,sidebar}.rs, src/panel/{panel,file_list,folder_tree,tabs,address_bar}.rs, Cargo.toml, docs/prd.md(FR-21), README.md
  - **미처리 Deferred**: 라이트/다크 전환 UI, 사이드바 색상 상수 공용화, 커스텀 다크 스크롤바, T7 menu_font 캐싱(매 항목 생성), T1 relayout 캐시 갱신 순서(저위험)
- 2026-07-24: **사이드바 폭 드래그 조절 시 깜빡임 수정 (systematic-debugging)**
  - **무엇을**: 메인 창(`window.rs`) 스타일에 `WS_CLIPCHILDREN`을 추가. import 1줄 + 창 생성 스타일 `WS_OVERLAPPEDWINDOW | WS_CLIPCHILDREN`(사유 주석 포함).
  - **왜(근본 원인)**: 자식 창들(패널 `panel.rs:366`, 사이드바)은 이미 `WS_CLIPCHILDREN|WS_CLIPSIBLINGS`를 갖췄지만 **부모인 메인 창만 `WS_CLIPCHILDREN`이 누락**돼 있었다. 배경 브러시가 흰색(`COLOR_WINDOW+1`)이고 별도 `WM_ERASEBKGND` 핸들러가 없어 `DefWindowProc`가 흰색으로 배경을 지운다. 사이드바 경계 드래그 → `drag_sidebar`→`layout_children`이 자식(사이드바 `MoveWindow` + 패널 `DeferWindowPos`)을 매 프레임 이동시킬 때, 부모가 자식이 덮은 영역까지 흰색으로 erase한 뒤 자식이 다시 그려져 흰색 깜빡임이 발생했다. 자식들만 clip 스타일을 갖추고 부모만 빠뜨린 전형적 실수.
  - **검증 결과**: cargo build / clippy(-D warnings) 통과. GUI 렌더링 문제라 자동 회귀 테스트 불가(RED 불능) — 수동 재현: `cargo run` → 사이드바-탐색기 경계선을 좌우로 드래그해 폭을 반복 조절하며 깜빡임 소멸 확인 필요(**HUMAN-VERIFY 잔여**). 잔여 깜빡임이 있으면 사이드바 `MoveWindow`와 host `relayout`을 단일 배치로 묶는 추가 조사(H2) 검토.
  - **변경 파일**: src/app/window.rs
- 2026-07-24: **워크스페이스 사이드 패널 + 워크스페이스별 탐색기 (T1~T9)**
  - **무엇을**: 창을 좌측 워크스페이스 목록 / 우측 탐색기로 분리하고, 워크스페이스마다 독립된 탐색기 화면(분할·패널·탭·히스토리)을 갖도록 확장. ① T1 워크스페이스 목록 모델(`app/workspace.rs` 신규 — add/rename/remove/reorder/set_active/set_subtitle, 자동 이름은 사용 중이지 않은 최소 번호, `elide_path`) ② T2 `LayoutHost` 배치 영역 주입(`area` 필드, `set_area`/`set_visible`/`destroy_all` 추가, `relayout`/`close_active`/`drag_move`의 `parent` 제거, `client_rect` pub 승격) ③ T3 세션 스키마 v2(`Session{sidebar, active_workspace, workspaces[{name,layout,panels,active_panel}]}`, 워크스페이스 단위 무결성 검증 + 사이드바 폭만 클램프) ④ T4 사이드바 창(`app/sidebar.rs` 신규 — 커스텀 창 클래스 + WM_PAINT 직접 그리기, 다크 2줄 카드·헤더·`+` 버튼·선택 강조 바·시스템 폴더 아이콘·휠 스크롤·hover) ⑤ T5 다중 호스트·전환·지연 생성·복원(`EntryState::Pending/Live`) ⑥ T6 이름 변경(인라인 EDIT+서브클래스)·삭제(확인 대화상자·`destroy_all`)·드래그 정렬(8px 임계·중앙 기준 삽입선)·컨텍스트 메뉴·메뉴 바 "워크스페이스(&W)" ⑦ T7 사이드바 접기(`Ctrl+B`·메뉴·상단 ◧ 버튼)·경계 드래그 폭 조절(160~480)·상태 저장 ⑧ T8 성능 실측·회귀 ⑨ T9 부제 자동 갱신이 인라인 편집을 취소하던 결함 수정(F-7 BLOCKER — `refresh_items`/`set_items` 분리)
  - **왜**: PRD 갱신(2026-07-24 승인)으로 FR-15~FR-20·NFR-8 추가 — Out of Scope였던 "세션 프로필 다중 관리"를 요구로 채택하고 "다크 모드·테마" 제외는 *앱 전체 테마 전환 기능*으로 한정(사이드바 고정 다크는 허용). plan: docs/plans/2026-07-24-workspace-sidebar.md
  - **핵심 결정**: ① 워크스페이스별 컨테이너 창을 만들지 않고 `LayoutHost`에 배치 영역을 주입(D1 — `compute_rects`가 이미 임의 원점을 지원하고 히트테스트가 같은 좌표계라 diff·회귀 위험이 작다) ② 비활성 워크스페이스는 지연 생성 후 숨김 유지(D2 — 전환 즉시성·상태 보존) ③ 사이드바는 시스템 컨트롤이 아니라 직접 그리기(D3) ④ 부제는 패널의 `WM_APP_PATH_CHANGED` 신호 + 기존 `WM_APP_SESSION_COLLECT` 조회로 갱신하되 **활성 패널이 보낸 것만** 반영(D6) ⑤ F2·Delete·방향키는 사이드바 `WM_KEYDOWN` 로컬 처리 — 액셀러레이터는 포커스와 무관하게 소비돼 주소창 Delete가 회귀하기 때문(D16) ⑥ 미방문 워크스페이스 부제는 저장된 `active_panel` 기준으로 계산(D18) ⑦ v1 세션은 마이그레이션 없이 폴백(사용자 결정) ⑧ 사이드바 표시 갱신은 **편집 보존 여부에 따라 두 API로 분리**(`refresh_items` 부제 갱신 전용 / `set_items` 구조 변경 전용) — 부제 자동 갱신이 인라인 편집을 취소하던 결함(F-7 BLOCKER B1)의 근본 해결이며, 플래그 대신 함수를 나눠 호출부가 판단을 미루지 못하게 했다
  - **검증 결과**: cargo build/clippy(-D warnings)/fmt/test 전부 통과 — 단위 75 + 통합 2. **실측(release 439KB, APPDATA 리다이렉트로 사용자 설정 미오염): 시작 중앙값 258ms(<1s) · 1워크스페이스·2패널 유휴 23.6MB(<50MB) · 워크스페이스 5개 방문 유휴 24.1MB(<100MB, NFR-8)**. 지연 생성 교차 검증: 5개 세션 시작 시 패널창 1개·스레드 5 → 5개 방문 후 5개·9 → 4개 삭제 후 1개·5로 완전 회수(누수 없음), 미방문 워크스페이스 데이터도 손실 없이 재저장. task마다 spec+quality 이중 리뷰(T6에서 MAJOR 2·MINOR 1 발견 → 수정 후 재리뷰 통과). **사이드바·전환·편집·드래그의 화면 동작은 HUMAN-VERIFY 잔여**
  - **변경 파일**: src/app/{workspace,sidebar}.rs(신규), src/app/{window,layout_host,settings,menu,mod}.rs, src/panel/panel.rs, src/fs/icons.rs, docs/prd.md, README.md
  - **미처리 Deferred**: FR-13 숨김 파일 토글(Could), FR-14 분할 프리셋(Could), 트리→목록 양방향 동기화, shell_menu `pidls[0]` 암묵 계약 문서화, 사이드바 커스텀 다크 스크롤바, 인라인 편집 EDIT 다크 스타일링
- 2026-07-23: **v1 part2 완료 — 트리·셸 메뉴·감시·세션·성능 (T1~T5, v1 완성)**
  - **무엇을**: part1 코어 위에 잔여 PRD 요구 전부 구현. ① T1 폴더 트리(`SysTreeView32` 패널별 토글 — 드라이브 루트, TVN_ITEMEXPANDING 지연 확장 시 워커 열거 재사용, 확장은 보류(반환 1) 후 완료 시 `apply_expand`(차용 해제 후), 선택 시 활성 탭 navigate) ② T2 셸 컨텍스트 메뉴(`shell_menu` — SHParseDisplayName→IShellFolder→GetUIObjectOf(IContextMenu)→QueryContextMenu→TrackPopupMenuEx→InvokeCommand, IContextMenu2/3 소유자 메시지 포워딩 thread_local, 0개 선택은 CreateViewObject 배경 메뉴, 배선은 WM_CONTEXTMENU) ③ T3 변경 감시(`DirWatcher` — RDCW overlapped 발행 상시 유지 + 조용 300ms 디바운스, 정지 이벤트, Drop은 회수 스레드에 join 위임, F5=IDM_REFRESH 공용 refresh) ④ T4 세션(`settings.rs` D15 스키마 serde + parse 무결성 검증, TreeShape 스냅숏/재구성, WM_APP_SESSION_COLLECT/RESTORE 같은 스레드 SendMessage 포인터 계약, WM_CLOSE 저장·시작 복원·창 배치 모니터 검사) ⑤ T5 성능 실측(코드 수정 0)
  - **왜**: PRD FR-8(Must)·FR-9~12(Should)·NFR-1~3·NFR-7 충족 → v1 Must 8/8 + Should 4/4 완성. plan: docs/plans/2026-07-23-file-explorer-part2.md (분할 plan 2/2)
  - **핵심 결정**: lib 타깃 도입(bin+lib — tests/ 통합테스트 전제, main.rs mod→use), D18 세션 복원 시 삭제 경로는 is_dir 검사 없이 열거 실패 경로 위임(리뷰 B1 — UI 스레드 블로킹 금지), 디바운스는 "조용 300ms까지 흡수"(RDCW가 발행 사이 변경을 OS 버퍼링하는 특성 반영), Cargo.toml windows feature 4종 추가(Shell_Common·Security·System_IO·System_Threading — 신규 의존성 아님)
  - **검증 결과**: cargo build/clippy(-D warnings)/fmt/test 전부 통과 — 단위 43 + 통합 2(watcher, arming 루프로 시동 레이스 흡수·3회 연속 통과). 성능 실측(release 382KB, APPDATA 리다이렉트로 사용자 설정 미오염): **NFR-1 시작 209ms 중앙값 · NFR-2 2패널 유휴 20.8MB · NFR-3 10만 파일 진입 probe 82회 timeout 0** — 전부 PASS. task마다 spec+quality 이중 리뷰(T3 MAJOR 1·T4 BLOCKER 1 발견·수정 후 재리뷰 OK). **UI 동작(트리·셸 메뉴·자동 갱신·세션 복원 화면 확인)은 HUMAN-VERIFY 잔여**
  - **변경 파일**: src/lib.rs(신규), src/fs/{shell_menu,watcher}.rs(신규), src/panel/folder_tree.rs(신규), src/app/settings.rs(신규), tests/watcher.rs(신규), src/main.rs, src/app/{window,menu,layout,layout_host,mod}.rs, src/panel/{panel,file_list,tabs,mod}.rs, src/fs/{mod,icons}.rs, Cargo.toml
  - **미처리 Deferred**: FR-13 숨김 파일 토글(Could), FR-14 분할 프리셋(Could), 트리→목록 양방향 동기화(D14), shell_menu items_menu의 pidls[0] 암묵 계약 문서화(T2 리뷰 m1)
- 2026-07-23: **v1 part1 완료 — 멀티 분할 파일 탐색기 코어 (T1~T6)**
  - **무엇을**: Rust(windows-rs 0.62)/Win32 직접 호출로 파일 탐색기 코어 구현. ① 프로젝트 셋업(cargo·매니페스트 링커 임베드: PMv2 DPI/공용컨트롤 v6/longPathAware, build-dep 없이 build.rs 링커 플래그) ② 자유 분할 레이아웃 트리(순수 로직 split/close/set_ratio/compute_rects, 최소 120px 클램프) ③ 렌더링·스플리터 드래그·분할 명령(메뉴+Ctrl+\\·Ctrl+Shift+\\·Ctrl+Shift+W, DeferWindowPos 일괄 배치, 부모 히트테스트 스플리터) ④ 파일 목록(SysListView32 가상 모드, 워커 스레드 열거 FindFirstFileExW+`\\?\` 긴경로, 시스템 아이콘 공유+확장자/경로 캐시, StrCmpLogicalW 폴더우선 정렬) ⑤ 네비게이션(주소창 Edit 서브클래스 Enter·버튼 ←→↑, 탭별 히스토리 peek/커밋 분리, pending-커밋 모델 — 실패 시 현 위치 유지, ShellExecuteExW 실행, Alt+←/→/↑) ⑥ 패널별 탭(TabsModel 순수 모델+WC_TABCONTROL, Ctrl+T 복제/Ctrl+W 닫기 — 마지막 탭은 패널 닫기 연결)
  - **왜**: PRD(docs/prd.md) FR-1~7 Must + FR-12 일부 충족. plan: docs/plans/2026-07-23-file-explorer-part1.md (분할 plan 1/2)
  - **어떻게(핵심 결정)**: 창 상태는 `Box<RefCell<...>>` in GWLP_USERDATA(재진입 별칭 안전망 — 1차 방어는 핸들러 필터), LVM_SETITEMCOUNT는 RefCell 차용 해제 후 적용(재진입 표시 누락 방지), unsafe는 소단위 헬퍼 격리+안전성 주석, windows-rs 0.62 개명 주의(`Error::from_win32`→`from_thread`), GetMessageW는 `.0 > 0` 판정
  - **검증 결과**: cargo build/clippy(-D warnings)/fmt/test 전부 통과 — 단위테스트 35/35 (layout 10·enumerate 4·file_list 5·history 6·normalize 5·tabs 5). release exe 252KB. task마다 spec+quality 이중 리뷰(MAJOR 총 5건 발견·전부 수정 후 재리뷰 OK). **UI 동작(창·분할·드래그·탭·탐색)은 빌드 통과 상태이며 화면 확인은 사용자 필요(HUMAN-VERIFY)**
  - **변경 파일**: Cargo.toml, build.rs, app.manifest, src/main.rs, src/app/{window,layout,layout_host,menu}.rs, src/panel/{panel,file_list,address_bar,history,tabs}.rs, src/fs/{enumerate,icons}.rs
  - **미처리 Deferred**: part2 실행(docs/plans/2026-07-23-file-explorer-part2.md — 트리·셸 메뉴·감시·세션·성능), FR-13 숨김 파일 토글(Could), FR-14 분할 프리셋(Could), 아이콘 비동기 프리페치(part2 T5 성능 미달 시)
- 2026-07-29: **탭 스트립 통합·아이콘 버튼 프레임 제거·분할 패널 테두리 (T1~T4)**
  - **경위**: 사용자가 실행 화면과 Windows 11 탐색기·VS Code 스크린샷을 대조해 두 가지를 지적했다 — ① 탭이 `[제목]`·`[×]` 두 개의 사각 버튼으로 갈라져 보임 ② 화면을 분할해도 패널 경계가 안 보임. 표시 방식만 바꾸는 작업으로 범위를 한정했다(탐색·탭 조작 로직 무변경).
  - **분할 경계가 안 보였던 진짜 이유**: 테두리가 아예 없어서가 아니라 **활성 패널 1개에만 있었기 때문**이다. 스플리터 틈 4px는 `WINDOW_BG`(0x1B)인데 패널 배경이 `SURFACE_BG`(0x1E)라 명도차가 3단계뿐이라 틈이 눈에 띄지 않았다. 틈 색을 밝히는 대신 패널마다 테두리를 두르는 쪽을 택했다 — 창 가장자리 쪽 경계까지 표시되기 때문.
  - **탭을 위젯 2개로 두지 않은 이유**: egui `Button`은 자기 프레임·여백을 스스로 그려, 두 버튼을 붙여도 한 덩어리로 보이지 않는다. 영역만 잡고 내용을 painter로 직접 그리는 방식(타이틀바가 이미 쓰던 방식)으로 통일했다.
  - **기각**: 분할 버튼 아이콘을 phosphor 글리프로 교체하는 안 — 요청은 프레임 제거였고 현행 직접 그린 아이콘도 충분히 명확해 최소 수정 원칙을 택했다. 대신 `MenuButton`은 `Button` 값을 요구해 공통 아이콘 버튼과 맞지 않아 `Popup::menu`로 바꿨다(설정 버튼 선례).
  - **회귀 방어**: "×를 눌렀는데 탭 전환만 되는" 결함이 조용히 들어올 수 있어, 닫기 판정을 `ui.interact` 등록 순서와 `is_close_hit`(순수 함수, 단위 테스트) 두 겹으로 뒀다. 다만 **배선 자체(그 함수를 실제로 부르는가)는 순수 테스트가 잡지 못해** 화면 확인 항목으로 남겼다.
  - **화면 확인**: 사용자가 실행 화면에서 탭 통합·버튼 프레임 제거·비활성 탭 × 클릭·분할 테두리 4가지를 확인해 "잘됨" (F-8 통과). 데스크톱 GUI라 자율 검증으로는 소스 대조(26행)까지만 가능했고 픽셀·조작 결과는 이 확인이 정본이다.
  - **Deferred**: ① 탭 폭 고정(Windows 11식) 여부는 좁은 분할에서 화면을 보고 판단 ② `nav_button` 활성 경로가 `theme::TEXT`·16px을 수동 재기술 — `widgets::icon_button` 래퍼로 줄일지, `DEFAULT_ICON_PX`를 pub으로 올릴지 함께 판단
- 2026-07-29: **패널 안 스크롤 영역 ID 충돌 수정 (화면에 빨간 경고 표시)**
  - **증상**: 실행하면 패널마다 `First use of ScrollArea ID XXXX` / `Second use of ScrollArea ID XXXX` 빨간 문구와 빨간 테두리가 떴다. egui가 디버그 빌드에서 그리는 **위젯 ID 충돌 경고**로, 릴리스에서는 안 보이지만 **스크롤 위치가 서로 섞이는 실제 결함**이다.
  - **원인**: egui는 `UiBuilder`에 이름(`id_salt`)을 주지 않으면 하위 영역에 전부 `"child"`라는 **같은 이름**을 준다(자동 증가 번호는 `unique_id`에만 들어가고 `id`에는 안 들어간다). `ScrollArea`도 이름을 안 주면 `"scroll_area"` 고정이라, **한 패널 안의 탭 스트립 스크롤과 파일 목록 스크롤이 완전히 같은 ID**가 됐다.
  - **회귀가 아니라 원래 있던 결함**: 타이틀바 작업 브랜치는 `panel.rs`·`tabs.rs`·`file_list.rs`·`tree.rs`를 한 줄도 건드리지 않았다(`git diff`로 확인). 디버그 빌드를 띄우면서 처음 눈에 띈 것이다.
  - **수정**: `panel.rs`의 트리·콘텐츠 하위 영역에 이름(`id_salt("tree")`/`("content")`)을 부여. 프로덕션 변경은 2줄.
  - **회귀 테스트를 실제로 만들 수 있었다**: 헤드리스 `egui::Context::run_ui`로 패널을 한 프레임 그린 뒤 **그려진 도형에서 egui의 경고 텍스트를 찾는** 방식(트리 표시/숨김 두 상태). 수정 전 RED(`ScrollArea ID EF0B` 충돌) → 수정 후 GREEN. UI 코드라 테스트가 어렵다고 지레 포기하지 않아도 되는 사례.
- 2026-07-29: **커스텀 타이틀바 — 사이드바 토글·설정 버튼 (T1~T5)**
  - **경위**: "타이틀바에 버튼을 추가해 달라"는 요청이었는데, OS가 그리는 제목 표시줄에는 앱이 그릴 수 없어 **창 장식을 끄고 타이틀바 전체를 직접 그리는** 작업이 됐다. 사용자에게 트레이드오프(창 그림자·둥근 모서리·스냅 레이아웃 팝업 상실)를 먼저 확인받고 진행했다. PRD에 FR-22를 새로 넣었다.
  - **가장 값비싼 교훈 — egui의 겹친 위젯 판정**: 타이틀바 배경을 끌기 감지용으로 잡고 그 위에 버튼을 그리면, **버튼을 누른 프레임에 배경의 끌기 신호도 함께 나간다.** egui는 클릭 위젯과 끌기 위젯을 각각 독립으로 히트 판정하고 `is_pointer_button_down_on()`은 둘 중 하나만 걸려도 참이기 때문이다. 그러면 OS 창 이동 루프가 열려 그 클릭이 삼켜진다. **같은 실수를 크기 조절에서 한 번 더 했다**(상단 4px 띠를 창 폭 전체에 적용 → 캡션 버튼 위쪽이 안 눌림). 두 번 다 품질 리뷰가 잡았고, 해법은 같았다 — **감지 영역에서 버튼 자리를 빼는 것**.
  - **모서리는 예외로 뒀다**: 위쪽 변은 버튼에 양보하되 좌·우상단 모서리는 크기 조절이 가져간다. 모서리까지 버튼에 주면 대각선으로 창을 잡을 자리가 없어진다.
  - **계획의 가정이 실측으로 뒤집힌 사례**: 계획 단계에는 "최외곽 4px는 전부 크기 조절 우선 — 버튼은 남은 영역으로 누를 수 있다"고 적었으나, 실제로는 클릭이 삼켜져 그 가정이 반증됐다. plan에 정정 기록.
  - **기각한 것**: `TITLEBAR_BG` 상수(기존 `WINDOW_BG`와 값이 같아 별칭일 뿐), 제목 말줄임 전용 함수(egui `Label::truncate()`가 폭 기준으로 처리 — 탭 제목이 문자 수 기준인 것은 스크롤 영역 안이라 폭을 쓸 수 없어서였다).
  - **화면 확인 완료**(2026-07-29): 데스크톱 앱이라 자동 캡처 수단이 없어 타이틀바 모양·창 조작은 사용자 확인에 맡겼고, 실행 화면 확인 결과 이상 없음. 확인 목록은 plan의 Verification Strategy 참조.
  - **미처리 Deferred**: 설정 팝업 5개 항목(설정·업데이트·릴리즈 노트·오픈소스 라이선스·정보)의 실제 기능, 창 그림자·둥근 모서리 복원(eframe이 winit 확장에 닿을 길이 없어 보류)
- 2026-07-29: **네 방향 분할 — 탭 스트립 분할 버튼 (T1~T3)**
  - **경위**: 이미지 3장으로 요구를 받았다(현재 화면 / 버튼이 추가된 목표 / 그 버튼의 드롭다운). PRD를 먼저 갱신하고(FR-1에 네 방향 배치·진입점, FR-12에 `Ctrl+Alt+방향키` 4종 — 사용자 승인) 계획을 세웠다. plan: `docs/plans/2026-07-28-split-4way.md`.
  - **결정 배경 (커밋에 없는 것)**:
    - **축과 배치를 나눴다**(D1) — `LayoutTree`는 "왼쪽·위쪽" 같은 방향을 모르고 `SplitDir`(축) + `SplitPlace`(앞/뒤)만 받는다. 사용자 관점의 네 방향(`SplitTo`)은 `ui::menu`에서 **한 번만** 변환한다. 4방향을 그대로 트리에 내렸다면 호출부마다 같은 변환이 반복됐을 것이다.
    - **`split`에 인자를 추가**하고 두 번째 메서드를 만들지 않았다(D2) — 메서드가 둘이면 다음 작업자가 어느 것을 쓸지 헷갈리고 하나는 레거시가 된다. Rust는 시그니처를 바꾸면 호출부 20곳이 전부 컴파일 오류로 드러나 누락이 숨지 못한다.
    - **아이콘을 글리프가 아니라 painter로 그렸다**(D8) — `◫`(U+25EB)가 맑은 고딕에 있는지는 **화면을 띄워야** 알 수 있고 없으면 두부(□)가 된다. "폰트에 없으면 대체 문자로" 같은 분기를 계획에 남기면 구현자가 화면을 보고 판단해야 하는데, 그 판단 결과가 곧 시각 명세를 바꾼다 — 분기 자체를 없앴다.
    - **기존 단축키의 뜻을 유지**했다(D4) — `Ctrl+\`는 종전 "좌우 분할"이 곧 오른쪽이었으므로 오른쪽 분할, `Ctrl+Shift+\`는 아래쪽 분할로 이었다. PRD FR-12를 깨지 않으면서 4종을 더한 것.
  - **알아둘 함정 (다음 세션용)**: ① **`splitter.rs`가 누른 좌표의 패널을 활성으로 만든다** — 그래서 "버튼이 속한 패널을 분할한다"(D3)를 화면으로 확인하면 `split_active`로 잘못 구현해도 같아 보인다. 비활성 id로 `split_panel`을 부르는 **단위테스트**가 이 계약을 잡는 유일한 수단이다. ② 보기 메뉴와 버튼 팝업이 `menu::split_items` **같은 함수**를 부른다 — 두 곳에 문구를 따로 적으면 다음 수정에서 반드시 갈라진다. ③ `Sides::shrink_left()`는 우측을 먼저 확보하고 좌측을 남는 폭으로 제한한다 — 탭이 늘어도 버튼이 밀려나지 않는 이유.
  - **세션 스키마는 건드리지 않았다** — 저장되는 것은 "어느 쪽에 넣었나"가 아니라 트리 구조(first/second)라, 왼쪽·위쪽 분할 결과도 기존 v2로 그대로 표현된다. `settings.rs` diff 0을 F-3에서 확인했다.
  - **미확인 (F-8 대기)**: 버튼이 실제 오른쪽 끝에 보이는지 · 아이콘 모양 · 팝업 배경이 앱 팔레트인지 · 팝업 선택이 그 패널을 나누는지 · 보기 메뉴 네 항목 · **탭 스트립 회귀**(탭 전환·닫기·새 탭·좁은 폭 겹침 — `Sides`로 재구성했으므로) — 데스크톱 창이라 자동 캡처가 불가해 사용자 확인이 남았다. 전체 목록은 plan의 F-8 인계 절이 정본이다.
- 2026-07-28: **egui 전면 이식 part2 — 트리·워크스페이스·메뉴·감시·세션·실측·진입점 승격 (T1~T7)**
  - **경위**: part1(탐색 코어) 완료 후 "시작" 지시로 part2 전량 실행. plan: `docs/plans/2026-07-26-egui-migration-part2.md`. **T7의 구 Win32 소스 삭제만 사용자가 보류**를 택해(삭제 목록 제시 후 결정) 진입점 승격까지만 수행했다 — 소스는 남되 실행 파일에서는 쓰이지 않는다.
  - **결정 배경 (커밋에 없는 것)**:
    - **egui 0.35 API가 plan과 달랐다** — plan D2가 적은 `SidePanel::left`는 이 버전에 없고 `Panel::left`로 통합됐다(폭 클램프는 `size_range`). 계획 문서의 API 이름은 그대로 믿지 말고 버전 소스로 확인해야 한다.
    - **뷰 보관 키를 인덱스 대신 `WorkspaceId`로** 바꿨다(D12) — 순서 변경·삭제로 인덱스가 흔들리면 뷰가 엉뚱한 워크스페이스에 붙는다. 사이드바 조작이 있는 목록에서 인덱스 키는 언제나 함정이다.
    - **사이드바가 조작을 `Option` 하나로 돌려주던 것을 `Vec`으로** 바꿨다(D12 ③) — 이름 편집 커밋과 다른 항목 클릭이 한 프레임에 겹치면 하나가 조용히 사라졌다(리뷰가 잡음). **한 프레임에 조작이 둘 이상 나올 수 있는 위젯은 단일 슬롯으로 받으면 안 된다.**
    - **감시 갱신이 선택을 풀지 않게** `set_entries`가 같은 폴더 재열거면 선택을 이름 기준으로 되살린다 — 자동 갱신은 사용자가 조작하지 않아도 일어나므로, 없으면 다른 앱이 파일 하나 만들 때마다 고르던 것이 사라진다.
    - **최대화 중에는 창 위치·크기를 저장하지 않는다** — 그때의 사각형은 화면 전체라, 저장하면 다음 실행에서 되돌릴 "일반 크기"가 없어진다(plan Halt Forecast가 예고한 대안 그대로).
    - **`SharedResources` 묶기는 기각**했다 — part1이 T3에서 정리하라고 남긴 항목이지만, T3가 명령을 `ExplorerApp`에서 직접 처리해 패널·스플리터 인자가 하나도 늘지 않았다. 늘지 않는 한 묶어도 실익이 없다.
  - **알아둘 함정 (다음 세션용)**: ① `CollapsingHeader`의 본문 클로저는 **접힌 상태에서 호출되지 않는다**(egui 0.35 소스 확인) — 그 클로저가 곧 "처음 펼친 순간"이라 지연 로딩이 별도 장치 없이 된다. ② `ctx.egui_wants_keyboard_input()`은 이름과 달리 **포커스를 가진 위젯이 있으면 참**이다(텍스트 입력 한정이 아님) — 단축키를 막는 용도로는 오히려 안전한 방향. ③ **자동 입력 주입은 키보드도 안 먹는다** — `SendKeys`로 Ctrl+\를 보내도 분할이 일어나지 않았다(part1의 마우스 주입 한계와 같은 현상). 상태를 만들어 측정해야 하면 **세션 파일을 직접 쓰거나 임시 측정 빌드**를 쓰는 편이 빠르다.
  - **실측(release, 2026-07-28)**: 콜드 스타트 133ms(2회차 이후, Win32 253ms) · 유휴 메모리 2패널 **137.6MB**(NFR-2 150MB 충족, Win32 23.6MB) · **워크스페이스 5개 방문 124.1MB**(NFR-8 200MB 충족 — 워크스페이스당 약 0.8MB로 지연 생성이 실제로 작동) · 10만 파일 폴더 143.8MB · exe 4.56MB. 306자 긴 경로 열거는 회귀 테스트로 고정했다. DPI와 화면 표시는 미확인(HUMAN-VERIFY).
  - **측정 부작용**: 측정 중 앱을 반복 실행·종료하면서 `%APPDATA%\FileExplorer\settings.json`이 덮어써졌다. 이전에 구성해 둔 워크스페이스가 있었다면 유실됐다 — **다음부터 측정 전 백업**한다(중간부터는 백업 후 진행).
  - **미처리 (plan Deferred — plan.md 교체 시 유실되므로 여기 보존. 정본은 `docs/plans/deferred.md`)**: 구 Win32 UI 코드 제거(사용자 보류, 대상 목록은 plan Deferred) · 트리→목록 양방향 동기화 · 한글 폰트 서브셋(메모리 추가 절감 후보) · `app/`·`panel/` 모듈 재배치 · 사이드바 마지막 항목 뒤 여백 4px · **워크스페이스 Delete 키 배정**(사이드바가 키를 전역으로 봐서 미배정 — F2도 같은 성질) · **워크스페이스 키보드 위/아래 전환**(현행 대비 누락) · **T6 측정 재현 절차**(임시 빌드로 측정, 스크립트 미보존) · `debug-2026-07-24-dark-ownerdraw.md` 루트 위치 정리 · **master 미병합 커밋 다수**(병합 전략 미결정).
- 2026-07-26: **egui 전면 이식 part1 — 탐색 코어 (T1~T7)**
  - **경위**: "남은작업은?" → PoC 결론(기술적 가능·NFR-2 미달) 제시 → 사용자가 **"NFR-2 완화해서 이식"** 결정. 범위 확인에서 NFR-2 150MB·병행 유지 후 교체·현행 기능 전부 이식·plan 2분할을 선택했다. plan: `docs/plans/2026-07-26-egui-migration-part1.md`(part2는 별도 파일).
  - **PRD 변경(승인)**: NFR-2 50→150MB, NFR-8 100→200MB, 목적 문단의 구현 수단, FR-21의 "Win32 제약상 best-effort" 삭제. **기능 요구(FR)는 하나도 바뀌지 않았다** — 바뀐 건 비기능 수치 2개와 구현 수단 표현뿐. 근거·손익은 `docs/prd.md` 결정 이력 2026-07-26 참조.
  - **PoC 최대 리스크 해소**: PoC에서 "행 클릭이 안 잡힌다"고 본 것은 **자동 마우스 주입의 한계**였고, 사용자가 실제 마우스로 확인하니 hover·클릭·더블클릭·우클릭 셸 메뉴가 모두 정상이었다. 이식 착수의 전제가 이 확인이었다.
  - **덤으로 고친 실제 버그**: `to_extended_pattern`이 붙이는 `\\?\` 접두사는 Win32 경로 정규화를 **건너뛰게** 해서 그 뒤의 `/`가 구분자로 인식되지 않았다(주소창에 `C:/Users` 입력 시 열거 실패). 접두사를 붙이기 전에 구분자를 통일해 해결 — **현행 Win32 앱도 같은 함수를 쓰므로 함께 고쳐졌다**.
  - **알아둘 함정 (다음 세션용)**: ① `eframe::App::ui`가 주는 `Ui`는 배경이 없다 — 창 배경은 `clear_color`가 정하고 기본값이 하드코딩 회색이라, 팔레트를 적용하려면 `clear_color` 오버라이드 + `CentralPanel`이 둘 다 필요하다. ② **egui에서 겹치는 위젯은 나중에 등록된 쪽이 이긴다** — 목록 전체를 덮는 배경 감지 위젯을 뒤에 등록했다가 행 클릭을 통째로 가로챌 뻔했다(리뷰가 잡음). 같은 이유로 활성 패널 판정은 `interact`가 아니라 포인터 좌표로 한다. ③ 성능 수치는 **렌더 시간(`ui()` 내부)과 프레임 간격(vsync 포함)이 40배 차이** 난다 — 어느 지표인지 반드시 명시할 것(release 렌더 p95 0.63ms vs 간격 p95 26ms). ④ 셸 메뉴는 모달이라 그리기 클로저가 끝난 뒤에 띄워야 한다.
  - **실측(release)**: exe **3.87MB**(PoC 10.1MB — wgpu를 빌드에서 빼서 -62%), 콜드 스타트 2회차 **51ms**(현행 Win32 358ms), Working Set **129.9MB**(홈 폴더 기준, NFR-2 150MB 충족), 10만 파일 렌더 p95 0.63ms, GDI 누수 없음. **여유가 20MB뿐**이고 이건 트리·사이드바가 없는 상태라 최종 판정은 part2 T6.
  - **남은 것**: part2(트리·워크스페이스 사이드바·메뉴/단축키·감시·세션·NFR 실측·구 Win32 제거). part1 결과물은 `file_explorer_egui.exe`로 **현행 앱과 병행**하며, 화면 동작 확인(⏳ HUMAN-VERIFY 항목들)은 사용자 확인 대기 중이다. follow-up 3건(전역 자원 묶기→part2 T3, 탭 말줄임 grapheme, 앱 경고 문구 위치)은 plan `## Deferred / Follow-up`에 있다.
- 2026-07-25: **egui(eframe) 이식 가능성 PoC (T1~T5, 별도 바이너리)**
  - **무엇을**: `cargo run --bin egui_poc --features egui-poc`로만 빌드되는 실험 바이너리(`src/bin/egui_poc/`)를 만들어 ① 대량 항목 가상 스크롤 ② 시스템 아이콘 ③ Win32 셸 컨텍스트 메뉴를 실측 검증. 기존 Win32 앱은 동작 무변경(`format_size_kb`·`format_filetime` 가시성만 `pub` 승격 — bin은 별개 crate라 `pub(crate)` 불가).
  - **왜**: "egui로 적용해줘" 요청에 대해 전면 이식 전 판단 근거를 확보하기 위함. 사용자가 PoC 우선·셸 메뉴는 Win32 유지·판정은 현행 NFR 기준·의존성은 feature 격리를 선택. plan: docs/plans/2026-07-25-egui-poc.md
  - **결과 요지**: 기술적으로는 이식 가능(스크롤 p95 10.55ms·아이콘 텍스처 197개/4952항목·GDI 누수 0·셸 메뉴 실제 표시). 대가는 **메모리와 크기** — glow 백엔드 131MB / wgpu 359MB vs 현행 22MB, exe 0.42MB → 10MB. **NFR-2(50MB)는 어느 백엔드로도 미달**. 미해결 리스크는 egui_extras 테이블의 **행 클릭 이벤트 미감지**(더블클릭 진입·행 우클릭)로, 해결 못 하면 목록 UI를 직접 구현해야 한다.
  - **핵심 결정·함정**: ① eframe 0.35의 `App`은 `update`가 아니라 `logic`+`ui` 구조 ② 테이블 셀·`Label`의 기본 sense가 `hover`라 클릭이 안 잡힘 ③ `row.response()`는 행이 아니라 **마지막 셀** 응답 ④ 화면 변화가 없으면 `request_repaint`만으로 연속 렌더가 안 됨(스피너 같은 애니메이션 필요) ⑤ 첫 로드는 창 표시 파이프라인과 겹쳐 응답성 측정 불가 — F5 재열거로 분리 측정 ⑥ 렌더 백엔드 선택이 메모리를 2.7배 가름(D4를 앞당겨 비교).
  - **검증 결과**: 각 task cargo build/clippy(-D warnings)/fmt/test(75+2) 통과 + 실행 캡처·수치 실측. 기본 `cargo build`에 eframe 미포함 확인(`cargo tree -e normal`).
  - **후속(같은 날 "남은 이슈 진행")**: ② 메모리 분해 측정 ③ 텍스처 스로틀(렌더 최대 3096→522ms) ④ 아이콘 지연 조회(System32 로드 585→84ms) ⑤ 정렬 테스트 3개 — 완료. ① 행 클릭은 목록을 `ScrollArea::show_rows` 직접 구성으로 바꾸고 egui_extras를 제거했으나 **자동 마우스 주입으로는 재현되지 않아 수동 확인 대기**(egui는 포인터 좌표·행 폭을 정상 인지하고 상단 버튼 클릭은 동작). ⑥ AGENTS.md `Plan Location` stale 갱신은 승인 대기.
  - **메모리 결론**: glow+폰트 127MB / glow+폰트없음 **100MB** / wgpu 361MB. 폰트 기여 27MB, 백엔드 차 234MB, **바닥 100MB** — NFR-2(50MB)는 어떤 조합으로도 불가.
